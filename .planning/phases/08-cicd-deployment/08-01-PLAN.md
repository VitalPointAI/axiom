---
phase: 08-cicd-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.prod.yml
  - scripts/deploy.sh
  - scripts/healthcheck.sh
  - .env.example
autonomous: true
requirements:
  - CICD-02
  - CICD-03

must_haves:
  truths:
    - "Production Docker Compose brings up all 4 services (postgres, web, indexer, alembic migration) with correct networking"
    - "Deploy script SSHs to server, pulls latest code, and restarts services with zero-downtime rolling strategy"
    - "Health check script verifies all services are running and returns clear pass/fail"
    - "Rollback is possible by re-deploying previous commit"
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "Production-grade Docker Compose with all services, health checks, restart policies, and resource limits"
      contains: "services:"
    - path: "scripts/deploy.sh"
      provides: "SSH-based server deployment with pull, build, migrate, rolling restart"
      contains: "docker compose"
    - path: "scripts/healthcheck.sh"
      provides: "Post-deploy health verification for all services"
      contains: "curl"
  key_links:
    - from: "scripts/deploy.sh"
      to: "docker-compose.prod.yml"
      via: "docker compose -f docker-compose.prod.yml up"
      pattern: "docker.compose.*prod"
    - from: "scripts/healthcheck.sh"
      to: "docker-compose.prod.yml"
      via: "checks health endpoints defined in compose"
      pattern: "curl.*health"
---

<objective>
Create production Docker Compose configuration and server deployment scripts for SSH-based zero-downtime deployment.

Purpose: Establish the deployment target infrastructure — production compose file with proper networking, health checks, and resource limits, plus deployment and health check scripts that will be called by GitHub Actions.

Output: `docker-compose.prod.yml`, `scripts/deploy.sh`, `scripts/healthcheck.sh`, updated `.env.example`
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@docker-compose.yml
@indexers/Dockerfile
@web/Dockerfile
@.env.example

<interfaces>
Existing docker-compose.yml defines 3 services:
- web: Next.js on port 3003:3000, depends on postgres
- indexer: Python indexer service, depends on postgres
- postgres: PostgreSQL 16-alpine with health check

Existing Dockerfiles:
- web/Dockerfile: Multi-stage Node 20 build, standalone output, runs as nextjs user on port 3000
- indexers/Dockerfile: Python 3.11-slim, runs `python -m indexers.service`

Alembic migrations: db/migrations/ with alembic.ini, env.py, versions/

Environment variables needed (from .env.example + docker-compose.yml):
- POSTGRES_PASSWORD, DATABASE_URL
- NEARBLOCKS_API_KEY, ALCHEMY_API_KEY, ETHERSCAN_API_KEY
- COINGECKO_API_KEY, CRYPTOCOMPARE_API_KEY, FASTNEAR_API_KEY
- JOB_POLL_INTERVAL, SYNC_INTERVAL_MINUTES
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create production Docker Compose and update .env.example</name>
  <files>docker-compose.prod.yml, .env.example</files>
  <action>
Create `docker-compose.prod.yml` based on existing `docker-compose.yml` with production hardening:

1. **postgres service:**
   - Same as dev but add `shm_size: 256mb`, memory limits via `deploy.resources.limits` (512MB memory)
   - Volume mount for postgres_data (persistent)
   - Port binding to 127.0.0.1:5432 only (no external access)
   - Health check same as dev

2. **migrate service** (NEW — runs Alembic before app starts):
   - Uses same image as indexer (python base with alembic installed)
   - Command: `alembic -c db/migrations/alembic.ini upgrade head`
   - depends_on postgres healthy
   - restart: "no" (one-shot)
   - Same DATABASE_URL env var

3. **web service:**
   - Same build as dev
   - Add `deploy.resources.limits` (256MB memory, 0.5 CPU)
   - depends_on: postgres (healthy) AND migrate (service_completed_successfully)
   - Health check: curl localhost:3000/api/health with 30s interval
   - Add labels for service identification: `app.service=web`

4. **indexer service:**
   - Same build as dev
   - Add `deploy.resources.limits` (512MB memory, 0.5 CPU)
   - depends_on: postgres (healthy) AND migrate (service_completed_successfully)
   - Add labels: `app.service=indexer`

5. **All services:** Add `logging` config with json-file driver, max-size 10m, max-file 3

6. **Networks:** Define `axiom-net` bridge network, attach all services

7. Update `.env.example` to include ALL env vars needed for production:
   - Add CRYPTOCOMPARE_API_KEY (missing from current .env.example)
   - Add JOB_POLL_INTERVAL=5
   - Add SYNC_INTERVAL_MINUTES=15
   - Add NODE_ENV=production
   - Add section comments: "# Database", "# API Keys", "# Indexer Config", "# Deployment"
   - Add DEPLOY_HOST, DEPLOY_USER, DEPLOY_PATH vars for deployment config

Do NOT remove the existing `docker-compose.yml` — it remains the dev config.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && docker compose -f docker-compose.prod.yml config --quiet 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <done>docker-compose.prod.yml validates without errors; contains postgres, migrate, web, indexer services; all have health checks or restart policies; .env.example lists all required vars</done>
</task>

<task type="auto">
  <name>Task 2: Create deployment and health check scripts</name>
  <files>scripts/deploy.sh, scripts/healthcheck.sh</files>
  <action>
Create `scripts/deploy.sh` — the main deployment script called from GitHub Actions (or manually):

```
#!/usr/bin/env bash
set -euo pipefail

# Usage: ./scripts/deploy.sh [--rollback COMMIT_SHA]
#
# Deploys current code to production server via SSH.
# Expects these env vars: DEPLOY_HOST, DEPLOY_USER, DEPLOY_PATH, DEPLOY_SSH_KEY (path to key file)

# Parse arguments
ROLLBACK_SHA=""
while [[ $# -gt 0 ]]; do
  case $1 in
    --rollback) ROLLBACK_SHA="$2"; shift 2 ;;
    *) echo "Unknown arg: $1"; exit 1 ;;
  esac
done

# Validate required env vars
for var in DEPLOY_HOST DEPLOY_USER DEPLOY_PATH; do
  if [[ -z "${!var:-}" ]]; then
    echo "ERROR: $var is not set"; exit 1
  fi
done

SSH_CMD="ssh -o StrictHostKeyChecking=no"
if [[ -n "${DEPLOY_SSH_KEY:-}" ]]; then
  SSH_CMD="$SSH_CMD -i $DEPLOY_SSH_KEY"
fi
SSH_TARGET="$DEPLOY_USER@$DEPLOY_HOST"

echo "==> Deploying to $SSH_TARGET:$DEPLOY_PATH"

# Step 1: Pull latest code (or checkout specific commit for rollback)
if [[ -n "$ROLLBACK_SHA" ]]; then
  echo "==> Rolling back to $ROLLBACK_SHA"
  $SSH_CMD $SSH_TARGET "cd $DEPLOY_PATH && git fetch origin && git checkout $ROLLBACK_SHA"
else
  echo "==> Pulling latest from main"
  $SSH_CMD $SSH_TARGET "cd $DEPLOY_PATH && git pull origin main"
fi

# Step 2: Build new images
echo "==> Building Docker images"
$SSH_CMD $SSH_TARGET "cd $DEPLOY_PATH && docker compose -f docker-compose.prod.yml build --parallel"

# Step 3: Run migrations (one-shot container)
echo "==> Running database migrations"
$SSH_CMD $SSH_TARGET "cd $DEPLOY_PATH && docker compose -f docker-compose.prod.yml up migrate --exit-code-from migrate"

# Step 4: Rolling restart — restart web first (user-facing), then indexer
echo "==> Restarting web service"
$SSH_CMD $SSH_TARGET "cd $DEPLOY_PATH && docker compose -f docker-compose.prod.yml up -d --no-deps --build web"

echo "==> Waiting for web health check (30s)"
sleep 30

echo "==> Restarting indexer service"
$SSH_CMD $SSH_TARGET "cd $DEPLOY_PATH && docker compose -f docker-compose.prod.yml up -d --no-deps --build indexer"

# Step 5: Run health checks
echo "==> Running health checks"
$SSH_CMD $SSH_TARGET "cd $DEPLOY_PATH && bash scripts/healthcheck.sh"

echo "==> Deployment complete!"
```

Make it executable (`chmod +x`).

Create `scripts/healthcheck.sh` — runs on the server to verify services:

```
#!/usr/bin/env bash
set -euo pipefail

# Health check script — verifies all Axiom services are running
# Run on the server after deployment

COMPOSE_FILE="${1:-docker-compose.prod.yml}"
MAX_RETRIES=5
RETRY_DELAY=10

check_service() {
  local service=$1
  local container_status
  container_status=$(docker compose -f "$COMPOSE_FILE" ps --format json "$service" 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('Health','') or d.get('State',''))" 2>/dev/null || echo "not_found")
  echo "$container_status"
}

echo "=== Axiom Health Check ==="

FAILED=0

# Check postgres
echo -n "PostgreSQL: "
PG_STATUS=$(check_service postgres)
if [[ "$PG_STATUS" == *"healthy"* ]] || [[ "$PG_STATUS" == *"running"* ]]; then
  echo "OK"
else
  echo "FAIL ($PG_STATUS)"
  FAILED=1
fi

# Check web via HTTP health endpoint
echo -n "Web (Next.js): "
for i in $(seq 1 $MAX_RETRIES); do
  if curl -sf http://localhost:3003/api/health > /dev/null 2>&1; then
    echo "OK"
    break
  fi
  if [[ $i -eq $MAX_RETRIES ]]; then
    echo "FAIL (health endpoint unreachable after ${MAX_RETRIES} retries)"
    FAILED=1
  else
    sleep $RETRY_DELAY
  fi
done

# Check indexer is running
echo -n "Indexer: "
IDX_STATUS=$(check_service indexer)
if [[ "$IDX_STATUS" == *"running"* ]]; then
  echo "OK"
else
  echo "FAIL ($IDX_STATUS)"
  FAILED=1
fi

echo "========================="

if [[ $FAILED -eq 1 ]]; then
  echo "HEALTH CHECK FAILED"
  # Show logs for debugging
  docker compose -f "$COMPOSE_FILE" logs --tail=20 2>/dev/null || true
  exit 1
fi

echo "ALL SERVICES HEALTHY"
exit 0
```

Make it executable (`chmod +x`).

Key design decisions:
- Deploy script runs LOCALLY (in CI or dev machine), SSHs to the server
- Health check script runs ON the server
- Rolling restart: web first, wait for health, then indexer — postgres never restarts during deploy
- Rollback = re-deploy a specific git SHA
- No Docker registry needed — builds happen on the server (simpler for single-server setup)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && bash -n scripts/deploy.sh && bash -n scripts/healthcheck.sh && echo "SYNTAX OK"</automated>
  </verify>
  <done>Both scripts pass bash syntax check; deploy.sh handles SSH deployment with rolling restart and --rollback flag; healthcheck.sh checks postgres, web, and indexer with retry logic; both are executable</done>
</task>

</tasks>

<verification>
1. `docker compose -f docker-compose.prod.yml config` validates without errors
2. `bash -n scripts/deploy.sh` passes syntax check
3. `bash -n scripts/healthcheck.sh` passes syntax check
4. docker-compose.prod.yml defines postgres, migrate, web, indexer services
5. All services have health checks or appropriate restart policies
6. .env.example includes all required environment variables
</verification>

<success_criteria>
- Production Docker Compose validates and defines all 4 services with health checks
- Deploy script handles pull, build, migrate, rolling restart, and rollback
- Health check script verifies all services with retry logic
- .env.example documents all required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/08-cicd-deployment/08-01-SUMMARY.md`
</output>
