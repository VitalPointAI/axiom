---
phase: 06-reporting
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - reports/templates/base.html
  - reports/templates/capital_gains.html
  - reports/templates/income.html
  - reports/templates/tax_summary.html
  - reports/templates/t1135.html
  - reports/templates/inventory.html
  - reports/templates/business_income.html
  - reports/generate.py
  - reports/handlers/report_handler.py
  - reports/__init__.py
  - indexers/service.py
  - tests/test_reports.py
autonomous: true
requirements: [RPT-05, RPT-06]

must_haves:
  truths:
    - "PDF reports render via WeasyPrint from Jinja2 HTML templates without error"
    - "Tax summary PDF combines capital gains, income, T1135, and superficial loss summaries"
    - "PackageBuilder generates complete tax package in output/{year}_tax_package/ flat folder"
    - "ReportHandler job type wired into IndexerService for async report generation"
    - "All report files follow clear naming convention"
  artifacts:
    - path: "reports/templates/base.html"
      provides: "Shared HTML layout with @page CSS, header, footer, page numbers"
      min_lines: 30
    - path: "reports/generate.py"
      provides: "PackageBuilder rewrite — orchestrates all reports into tax package"
      exports: ["PackageBuilder"]
    - path: "reports/handlers/report_handler.py"
      provides: "ReportHandler job type for IndexerService"
      exports: ["ReportHandler"]
  key_links:
    - from: "reports/generate.py"
      to: "reports/capital_gains.py, reports/income.py, reports/ledger.py, reports/t1135.py, reports/superficial.py, reports/export.py, reports/inventory.py, reports/business.py"
      via: "PackageBuilder instantiates each report module and calls generate()"
      pattern: "CapitalGainsReport|IncomeReport|LedgerReport|T1135Checker"
    - from: "reports/handlers/report_handler.py"
      to: "reports/generate.py"
      via: "ReportHandler calls PackageBuilder.build()"
      pattern: "PackageBuilder"
    - from: "reports/templates/base.html"
      to: "WeasyPrint"
      via: "HTML(string=rendered_template).write_pdf()"
      pattern: "write_pdf"
---

<objective>
Build PDF templates (Jinja2 + WeasyPrint), the PackageBuilder orchestrator (generate.py rewrite), and the ReportHandler job type for async report generation.

Purpose: Completes the reporting phase by adding PDF output (RPT-06), packaging all reports into a deliverable tax package (RPT-05), and wiring report generation into the IndexerService job queue for async operation.

Output: HTML templates, PackageBuilder, ReportHandler. Complete tax package in `output/{year}_tax_package/`.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-reporting/06-CONTEXT.md
@.planning/phases/06-reporting/06-RESEARCH.md
@.planning/phases/06-reporting/06-01-SUMMARY.md
@.planning/phases/06-reporting/06-02-SUMMARY.md
@.planning/phases/06-reporting/06-03-SUMMARY.md
@.planning/phases/06-reporting/06-04-SUMMARY.md

@reports/engine.py
@reports/capital_gains.py
@reports/income.py
@reports/ledger.py
@reports/t1135.py
@reports/superficial.py
@reports/export.py
@reports/inventory.py
@reports/business.py
@indexers/service.py
</context>

<interfaces>
<!-- From all prior plans -->
From each report module (06-01 through 06-04):
```python
# All report modules follow this pattern:
class XxxReport(ReportEngine):
    def generate(self, user_id, tax_year, output_dir, year_end_month=12, ...) -> dict:
        # Returns summary dict with report-specific data
        # Writes CSV files to output_dir
```

From reports/engine.py:
```python
class ReportEngine:
    def write_csv(self, output_path, headers, rows) -> Path: ...
    # PDF helper to be added in this plan:
    # def write_pdf(self, output_path, template_name, context) -> Path: ...
```

From indexers/service.py — job handler registration pattern:
```python
# Handlers registered via: self._handlers['job_type'] = handler_instance
# Each handler has: def run(self, job_row, conn) -> dict
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Jinja2 HTML templates + WeasyPrint PDF generation method</name>
  <files>reports/templates/base.html, reports/templates/capital_gains.html, reports/templates/income.html, reports/templates/tax_summary.html, reports/templates/t1135.html, reports/templates/inventory.html, reports/templates/business_income.html, reports/engine.py</files>
  <action>
    1. Create `reports/templates/` directory.

    2. Create `reports/templates/base.html` — shared Jinja2 layout:
       - Use `{% block content %}{% endblock %}` for page content
       - CSS @page media for A4 paper, 1.5cm margins
       - Header: "Axiom Tax Report — {{ report_title }}" + "{{ tax_year }}"
       - Footer: "Page {{ page }} of {{ pages }}" via CSS counter
       - Body font: Arial, Helvetica, sans-serif (web-safe, no custom fonts)
       - Table styles: borders, alternating row colors, right-aligned numbers
       - Print-friendly: no background colors that waste ink

    3. Create child templates that `{% extends "base.html" %}`:

       a. `capital_gains.html`:
          - Title: "Capital Gains/Losses Report — {{ tax_year }}"
          - Summary section: Total Proceeds, Total ACB, Net Gain/Loss, Taxable Amount (50%)
          - Table of all disposals with columns matching CSV headers
          - Superficial loss rows highlighted with yellow background
          - If specialist_override: red footnote bar with flagged item count

       b. `income.html`:
          - Title: "Income Summary — {{ tax_year }}"
          - Summary: Total Income by source type (staking, vesting, airdrop)
          - Monthly breakdown table
          - Annual total row at bottom

       c. `tax_summary.html`:
          - Title: "Tax Summary — {{ tax_year }}"
          - Combined one-pager: Capital Gains summary, Income summary, T1135 status, Superficial Losses summary
          - Key numbers highlighted: Net Capital Gains, Total Income, Taxable Capital Gains (50%), T1135 Required?
          - Section for tax treatment mode (capital vs business vs hybrid)

       d. `t1135.html`:
          - Title: "T1135 Foreign Property Check — {{ tax_year }}"
          - Summary: Total Foreign Property Cost, Threshold ($100K), Required?
          - Per-token breakdown table
          - Self-custody section with "CRA position unclear" footnote

       e. `inventory.html`:
          - Title: "Inventory Holdings — {{ tax_year }}"
          - Table of current holdings with ACB, FMV, unrealized gain/loss

       f. `business_income.html`:
          - Title: "Business Income Statement — {{ tax_year }}"
          - Revenue section: Crypto Income, Capital Gains
          - Expense section: COGS (if applicable)
          - Net Business Income

    4. Add `write_pdf(self, output_path, template_name, context)` method to `ReportEngine` base class in `reports/engine.py`:
       ```python
       def write_pdf(self, output_path, template_name, context):
           from jinja2 import Environment, FileSystemLoader
           from weasyprint import HTML
           templates_dir = Path(__file__).parent / "templates"
           env = Environment(loader=FileSystemLoader(str(templates_dir)))
           template = env.get_template(template_name)
           html_content = template.render(**context)
           output_path = Path(output_path)
           output_path.parent.mkdir(parents=True, exist_ok=True)
           HTML(string=html_content, base_url=str(templates_dir)).write_pdf(str(output_path))
           return output_path
       ```

    IMPORTANT: Use web-safe fonts only (Arial, Helvetica, sans-serif). WeasyPrint 68.0 is installed. Jinja2 is installed. CSS paged media @page { size: A4; margin: 1.5cm; } handles pagination.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python3 -c "
from jinja2 import Environment, FileSystemLoader
from pathlib import Path
templates_dir = Path('reports/templates')
assert templates_dir.exists(), 'templates dir missing'
env = Environment(loader=FileSystemLoader(str(templates_dir)))
for name in ['base.html', 'capital_gains.html', 'income.html', 'tax_summary.html', 't1135.html', 'inventory.html', 'business_income.html']:
    t = env.get_template(name)
    print(f'OK: {name}')
print('All templates load successfully')
"</automated>
  </verify>
  <done>All 7 HTML templates created, load without error in Jinja2. write_pdf() method added to ReportEngine. CSS @page media configured for A4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PackageBuilder — orchestrate all reports into tax package</name>
  <files>reports/generate.py, reports/__init__.py, tests/test_reports.py</files>
  <behavior>
    - Test 1: PackageBuilder.build() calls all report modules and writes to output/{year}_tax_package/
    - Test 2: Output directory created with correct flat structure
    - Test 3: Both CSV and PDF files generated for each report
    - Test 4: Koinly export (year + full) included
    - Test 5: Accounting exports (QB, Xero, Sage, double-entry) included
    - Test 6: Tax treatment 'capital' skips COGS report
    - Test 7: Tax treatment 'business_inventory' includes COGS and business income
    - Test 8: Tax treatment 'hybrid' generates both views
    - Test 9: Gate check runs once at top level, not per-report
    - Test 10: build() returns manifest dict listing all generated files
  </behavior>
  <action>
    Rewrite `reports/generate.py` (discard all legacy SQLite code) with `PackageBuilder`:

    1. `PackageBuilder`:
       - `__init__(self, pool, specialist_override=False)`
       - `build(self, user_id, tax_year, output_base='output', year_end_month=12, tax_treatment='capital', excluded_wallet_ids=None)`:
         - Creates `output_base/{year}_tax_package/` directory
         - Runs gate check ONCE via ReportEngine._check_gate
         - Instantiates each report module with pool and specialist_override
         - Calls generate() on each, collecting summary dicts
         - Generates PDF for each report that has a template:
           - capital_gains_{year}.pdf
           - income_summary_{year}.pdf
           - t1135_check_{year}.pdf
           - tax_summary_{year}.pdf (combined summary)
           - inventory_holdings_{year}.pdf
           - business_income_{year}.pdf (if tax_treatment in ['business_inventory', 'hybrid'])
         - Conditionally includes:
           - COGS: only if tax_treatment in ['business_inventory', 'hybrid']
           - Business Income: only if tax_treatment in ['business_inventory', 'hybrid']
           - Superficial Loss: always (it's a Koinly-parity report)
         - Always includes:
           - Koinly export (year-specific + full history)
           - Accounting exports (QuickBooks, Xero, Sage, double-entry)
           - Transaction ledger
           - T1135 check
         - Returns manifest dict: {files: [list of paths], summaries: {report_name: summary_dict}, tax_year, output_dir}

    2. Keep `if __name__ == "__main__":` CLI entry point with argparse:
       - `--year` (default: current year - 1)
       - `--output` (default: 'output')
       - `--tax-treatment` (choices: 'capital', 'business_inventory', 'hybrid', default: 'capital')
       - `--year-end-month` (default: 12)
       - `--specialist-override` (flag)
       - Creates psycopg2 pool from config, calls PackageBuilder.build()

    3. Update `reports/__init__.py` to export PackageBuilder.

    Add TestPackageBuilder to tests/test_reports.py. Mock all report modules (patch their generate methods). Verify build() calls each module, creates output directory, and returns correct manifest. Use tmp_path fixture for output.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python3 -m pytest tests/test_reports.py::TestPackageBuilder -x -q</automated>
  </verify>
  <done>PackageBuilder orchestrates all reports, generates both CSV and PDF, handles tax treatment modes, and returns complete manifest. Legacy generate.py code replaced. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: ReportHandler — IndexerService job type for async report generation</name>
  <files>reports/handlers/__init__.py, reports/handlers/report_handler.py, indexers/service.py, tests/test_reports.py</files>
  <action>
    1. Create `reports/handlers/__init__.py` (empty).

    2. Create `reports/handlers/report_handler.py` with `ReportHandler`:
       - `__init__(self, pool)` — stores pool reference
       - `run(self, job_row, conn)`:
         - Extracts from job_row: user_id, cursor (JSON string with tax_year, tax_treatment, year_end_month, specialist_override, excluded_wallet_ids)
         - Parses cursor JSON for parameters
         - Instantiates PackageBuilder(pool, specialist_override)
         - Calls build(user_id, tax_year, ...)
         - Returns stats dict: {files_generated: N, output_dir: path}
         - On ReportBlockedError: returns {error: str(e), blocked: True} — does NOT raise (job should be marked failed with informative message)

    3. Wire into IndexerService in `indexers/service.py`:
       - Import ReportHandler (lazy import to avoid circular deps, matching existing handler patterns)
       - Register: `self._handlers['generate_reports'] = ReportHandler(self.pool)` in _register_handlers()
       - Priority: 3 (lower than verify_balances at 4, higher than indexing at higher numbers) — reports are user-requested, not automatic pipeline

    4. Add TestReportHandler to tests/test_reports.py:
       - Test run() with valid job_row produces stats dict
       - Test run() with blocked gate returns error dict without raising
       - Test that generate_reports job type is registered in service handler map

    IMPORTANT: Follow the same lazy import pattern used by ACBHandler and VerifyHandler in service.py. The handler takes pool in __init__, not in run().
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python3 -m pytest tests/test_reports.py::TestReportHandler -x -q</automated>
  </verify>
  <done>ReportHandler wired as generate_reports job type. Handles gate blocking gracefully. Registered in IndexerService. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /home/vitalpointai/projects/Axiom && python3 -m pytest tests/test_reports.py tests/test_fifo.py -x -q
```
Full test suite passes including all report tests from plans 06-01 through 06-05.
</verification>

<success_criteria>
1. All 7 HTML templates render via Jinja2 without errors
2. WeasyPrint generates PDFs from templates (A4, web-safe fonts, paged media)
3. PackageBuilder orchestrates all reports into output/{year}_tax_package/ flat folder
4. Tax treatment parameter controls which reports are included
5. ReportHandler registered as generate_reports job type in IndexerService
6. Gate blocking handled gracefully in handler (returns error dict, not exception)
7. Legacy generate.py fully replaced with PackageBuilder
8. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-reporting/06-05-SUMMARY.md`
</output>
