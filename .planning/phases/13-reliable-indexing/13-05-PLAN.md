---
phase: 13-reliable-indexing
plan: 05
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - api/routers/admin.py
  - api/routers/streaming.py
  - api/main.py
  - indexers/xrp_fetcher.py
  - indexers/akash_fetcher.py
  - tests/test_admin_api.py
  - tests/test_streaming_api.py
autonomous: true
requirements: [IDX-08, IDX-09]

must_haves:
  truths:
    - "SSE endpoint /api/stream/wallet/{wallet_id} pushes real-time transaction updates via PostgreSQL LISTEN/NOTIFY"
    - "Admin cost dashboard endpoint /api/admin/cost-summary returns monthly cost aggregation per chain/provider"
    - "Admin indexing status endpoint /api/admin/indexing-status returns chain sync health"
    - "XRP fetcher has cost tracking wired in and handles trust line token balances"
    - "Akash fetcher has cost tracking wired in and handles pagination correctly"
  artifacts:
    - path: "api/routers/admin.py"
      provides: "Cost dashboard + indexing status admin endpoints"
      exports: ["router"]
    - path: "api/routers/streaming.py"
      provides: "SSE streaming endpoint for real-time wallet updates"
      exports: ["router"]
    - path: "tests/test_admin_api.py"
      provides: "Tests for admin cost dashboard API"
    - path: "tests/test_streaming_api.py"
      provides: "Tests for SSE streaming endpoint"
  key_links:
    - from: "api/routers/streaming.py"
      to: "PostgreSQL LISTEN/NOTIFY"
      via: "psycopg2 LISTEN new_transactions + select.select for async polling"
      pattern: "LISTEN new_transactions"
    - from: "api/routers/admin.py"
      to: "api_cost_monthly view"
      via: "SELECT from api_cost_monthly"
      pattern: "api_cost_monthly"
---

<objective>
Create the API layer for Phase 13: SSE streaming endpoint for real-time wallet updates, admin cost dashboard, indexing status endpoints, and harden XRP/Akash fetchers with cost tracking.

Purpose: The frontend needs real-time transaction updates via SSE (replacing polling). The admin needs visibility into indexing costs and chain health. XRP/Akash fetchers need cost tracking wired in.
Output: Admin router, streaming router, updated fetchers, tests.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-reliable-indexing/13-CONTEXT.md
@.planning/phases/13-reliable-indexing/13-RESEARCH.md
@api/main.py
@api/routers/wallets.py
@api/dependencies.py
@indexers/xrp_fetcher.py
@indexers/akash_fetcher.py
@indexers/db.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSE streaming endpoint + admin cost/status API</name>
  <files>api/routers/admin.py, api/routers/streaming.py, api/main.py, tests/test_admin_api.py, tests/test_streaming_api.py</files>
  <behavior>
    - GET /api/stream/wallet/{wallet_id} returns text/event-stream with SSE events on new transactions
    - SSE sends keepalive comment every 5 seconds if no events
    - SSE filters pg_notify payloads by wallet_id
    - GET /api/admin/cost-summary returns JSON with monthly cost per chain/provider
    - GET /api/admin/cost-summary?chain=near filters by chain
    - GET /api/admin/indexing-status returns per-chain sync status (last_indexed_block, last_sync_time, enabled)
    - GET /api/admin/budget-alerts returns chains exceeding monthly_budget_usd
    - Admin endpoints require authenticated user (reuse get_current_user dependency)
  </behavior>
  <action>
1. Create `api/routers/streaming.py`:
   - `GET /api/stream/wallet/{wallet_id}` — SSE endpoint using FastAPI StreamingResponse with media_type="text/event-stream"
   - Implementation: open a dedicated psycopg2 connection (NOT from pool — long-lived), set AUTOCOMMIT isolation, LISTEN new_transactions. Use select.select([conn], [], [], 5.0) for non-blocking poll. On notification, parse payload JSON, filter by wallet_id match, yield SSE event. On timeout (5s), yield keepalive comment `: keepalive\n\n`.
   - Add query param `?since_block=N` to optionally replay recent transactions since a block height before starting live stream.
   - Ensure connection cleanup on client disconnect (try/finally with conn.close()).

2. Create `api/routers/admin.py` (new file — no existing admin.py):
   - `GET /api/admin/cost-summary` — query api_cost_monthly view, return JSON array of {chain, provider, call_type, month, call_count, total_cost_usd}. Optional query param `chain` to filter.
   - `GET /api/admin/indexing-status` — query chain_sync_config joined with latest indexing_jobs per chain to show: chain, enabled, fetcher_class, last_job_completed_at, last_job_status. Also query api_cost_log for last_api_call_at per chain.
   - `GET /api/admin/budget-alerts` — for each chain in chain_sync_config with monthly_budget_usd set, compare against current month's total in api_cost_monthly. Return chains where total > budget.
   - All admin endpoints require authentication via get_current_user dependency.

3. Register routers in `api/main.py`:
   - Import and include streaming router with prefix="/api/stream"
   - Import and include admin router with prefix="/api/admin"

4. Write tests:
   - `tests/test_admin_api.py`: Mock DB pool, test cost-summary returns aggregated data, test chain filter, test budget-alerts returns correct chains.
   - `tests/test_streaming_api.py`: Mock psycopg2 connection + pg_notify, test SSE yields event on matching wallet_id, test keepalive on timeout, test non-matching wallet_id filtered out.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_admin_api.py tests/test_streaming_api.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>SSE endpoint streams wallet updates via LISTEN/NOTIFY, admin endpoints show cost summary + indexing status + budget alerts, tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire cost tracking into XRP and Akash fetchers</name>
  <files>indexers/xrp_fetcher.py, indexers/akash_fetcher.py</files>
  <action>
Update both fetchers to accept optional `cost_tracker` parameter and wrap external API calls:

1. `indexers/xrp_fetcher.py`:
   - Add `cost_tracker=None` to __init__ parameter
   - In `_json_rpc()` method, wrap the requests.post call with `self.cost_tracker.track('xrp', endpoint_host, method)` if cost_tracker is set. The endpoint_host is the XRPL endpoint being called (extract hostname from URL).
   - In `get_balance()`, similarly wrap the RPC call.
   - Remove "stub" from class docstring — these fetchers are functional.

2. `indexers/akash_fetcher.py`:
   - Add `cost_tracker=None` to __init__ parameter
   - In `_get()` method, wrap requests.get with `self.cost_tracker.track('akash', 'cosmos_lcd', path_segment)` if cost_tracker is set.
   - In `get_balance()`, similarly wrap.
   - Remove "stub" from class docstring.

3. Both fetchers: ensure cost tracking is optional (no crash if cost_tracker is None). Use a simple pattern:
   ```python
   if self.cost_tracker:
       with self.cost_tracker.track(chain, provider, call_type):
           response = requests.get(...)
   else:
       response = requests.get(...)
   ```
   Or use a no-op context manager helper to avoid duplication.

This is a minor change to existing files — only adding the cost_tracker parameter and wrapping API calls. Do NOT refactor the fetcher logic itself.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -c "
from indexers.xrp_fetcher import XRPFetcher
from indexers.akash_fetcher import AkashFetcher
import inspect
# Check cost_tracker param exists
sig_xrp = inspect.signature(XRPFetcher.__init__)
sig_akash = inspect.signature(AkashFetcher.__init__)
assert 'cost_tracker' in sig_xrp.parameters, 'XRPFetcher missing cost_tracker'
assert 'cost_tracker' in sig_akash.parameters, 'AkashFetcher missing cost_tracker'
print('Both fetchers accept cost_tracker parameter')
# Check stub removed from docstring
assert 'stub' not in (XRPFetcher.__doc__ or '').lower(), 'XRP still says stub'
assert 'stub' not in (AkashFetcher.__doc__ or '').lower(), 'Akash still says stub'
print('Stub labels removed from docstrings')
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <done>XRP and Akash fetchers accept cost_tracker parameter, wrap API calls with cost tracking, "stub" label removed from docstrings.</done>
</task>

</tasks>

<verification>
- SSE endpoint yields events on pg_notify for matching wallet
- Admin cost-summary returns monthly aggregation
- Admin indexing-status shows per-chain health
- Budget alerts fire when cost exceeds threshold
- XRP/Akash fetchers track API costs
- All tests pass
</verification>

<success_criteria>
- Real-time wallet updates available via SSE at /api/stream/wallet/{id}
- Cost visibility via /api/admin/cost-summary with chain filtering
- Chain health via /api/admin/indexing-status
- Budget alerts via /api/admin/budget-alerts
- All fetchers support optional cost tracking
</success_criteria>

<output>
After completion, create `.planning/phases/13-reliable-indexing/13-05-SUMMARY.md`
</output>
