---
phase: 10-remaining-concerns-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/versions/007_price_cache_index.py
  - indexers/db.py
  - config.py
  - pyproject.toml
autonomous: true
requirements: [RC-02, RC-06, RC-09]

must_haves:
  truths:
    - "price_cache has a composite index on (coin_id, date) for range queries"
    - "Connection pool min/max are configurable via DB_POOL_MIN and DB_POOL_MAX env vars"
    - "pyproject.toml declares requires-python >= 3.11"
  artifacts:
    - path: "db/migrations/versions/007_price_cache_index.py"
      provides: "Alembic migration adding price_cache composite index"
      contains: "CREATE INDEX IF NOT EXISTS"
    - path: "config.py"
      provides: "DB_POOL_MIN, DB_POOL_MAX configuration"
      contains: "DB_POOL_MIN"
    - path: "pyproject.toml"
      provides: "Python version constraint and project metadata"
      contains: "requires-python"
  key_links:
    - from: "indexers/db.py"
      to: "config.py"
      via: "import DB_POOL_MIN, DB_POOL_MAX"
      pattern: "from config import.*DB_POOL"
---

<objective>
Add price_cache composite index (migration 007), make connection pool sizing configurable via env vars, and add python_requires to pyproject.toml.

Purpose: Improve query performance on price lookups, enable operational tuning of DB pool without code changes, enforce Python version requirement.
Output: Migration 007, updated config.py + db.py, updated pyproject.toml.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-concerns-remediation/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 007 + configurable pool + pyproject.toml</name>
  <files>db/migrations/versions/007_price_cache_index.py, config.py, indexers/db.py, pyproject.toml, tests/test_config_validation.py</files>
  <behavior>
    - Test: DB_POOL_MIN env var is read from config.py and defaults to 1
    - Test: DB_POOL_MAX env var is read from config.py and defaults to 10
    - Test: pool_stats() returns dict with minconn, maxconn keys
    - Test: pyproject.toml has requires-python field (manual verify inline)
  </behavior>
  <action>
1. Create Alembic migration 007_price_cache_index.py:
   - `op.execute("CREATE INDEX IF NOT EXISTS ix_price_cache_coin_date_desc ON price_cache (coin_id, date DESC)")` for efficient range queries
   - Confirm price_cache_minute already has ix_pcm_coin_ts; add `CREATE INDEX IF NOT EXISTS` as safety net
   - downgrade: `op.drop_index("ix_price_cache_coin_date_desc")`

2. Update config.py:
   - Add `DB_POOL_MIN = int(os.environ.get("DB_POOL_MIN", "1"))`
   - Add `DB_POOL_MAX = int(os.environ.get("DB_POOL_MAX", "10"))`
   - Add to validate_env(): include pool size validation (MIN <= MAX, both > 0)

3. Update indexers/db.py get_pool():
   - `from config import DATABASE_URL, DB_POOL_MIN, DB_POOL_MAX`
   - Change default args: `def get_pool(min_conn: int = DB_POOL_MIN, max_conn: int = DB_POOL_MAX)`
   - Add `pool_stats(pool)` function returning `{"minconn": pool.minconn, "maxconn": pool.maxconn, "available": len(pool._pool), "in_use": len(pool._used)}`

4. Update pyproject.toml:
   - Add `[build-system]` with setuptools
   - Add `[project]` table with name="axiom", version="1.0.0", requires-python=">=3.11"
   - Add `[tool.setuptools]` with `packages = []` (not installed as package)
   - Keep existing `[tool.ruff]` sections intact

5. Add tests to tests/test_config_validation.py:
   - test_db_pool_min_default: assert DB_POOL_MIN == 1 (or env var value)
   - test_db_pool_max_default: assert DB_POOL_MAX == 10 (or env var value)
   - test_pool_stats_returns_expected_keys: mock pool with _pool and _used attrs

6. Verify: `python -m pytest tests/test_config_validation.py -x -q && python -c "import tomllib; d=tomllib.loads(open('pyproject.toml').read()); assert d['project']['requires-python'] == '>=3.11'"`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_config_validation.py -x -q && python -c "import tomllib; d=tomllib.loads(open('pyproject.toml').read()); assert d['project']['requires-python'] == '>=3.11'" && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Migration 007 creates price_cache index, pool config reads from env vars with tests, pyproject.toml has requires-python >= 3.11, full test suite passes.</done>
</task>

</tasks>

<verification>
1. Migration test: `cd /home/vitalpointai/projects/Axiom && python -c "from db.migrations.versions import *"` — no import error
2. Config test: `python -c "from config import DB_POOL_MIN, DB_POOL_MAX; print(DB_POOL_MIN, DB_POOL_MAX)"`
3. pyproject.toml: `python -c "import tomllib; print(tomllib.loads(open('pyproject.toml').read())['project']['requires-python'])"`
4. Full suite: `python -m pytest tests/ -q`
</verification>

<success_criteria>
- Migration 007 exists and adds price_cache composite index
- DB_POOL_MIN/MAX configurable via env with sane defaults
- pool_stats() function available in indexers/db.py
- pyproject.toml has [project] table with requires-python >= 3.11
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-concerns-remediation/10-02-SUMMARY.md`
</output>
