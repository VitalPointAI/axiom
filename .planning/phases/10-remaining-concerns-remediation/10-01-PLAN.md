---
phase: 10-remaining-concerns-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/classifier/__init__.py
  - engine/classifier/near_classifier.py
  - engine/classifier/evm_classifier.py
  - engine/classifier/exchange_classifier.py
  - engine/classifier/writer.py
  - engine/acb/__init__.py
  - engine/acb/pool.py
  - engine/acb/engine.py
  - engine/acb/symbols.py
  - db/models/__init__.py
  - db/models/users.py
  - db/models/wallets.py
  - db/models/events.py
  - db/models/prices.py
  - db/models/classification.py
  - db/models/acb.py
  - db/models/verification.py
autonomous: true
requirements: [RC-01]

must_haves:
  truths:
    - "All existing imports from engine.classifier, engine.acb, and db.models continue to work unchanged"
    - "No file exceeds 400 lines after the split"
    - "Full test suite passes with zero import errors"
  artifacts:
    - path: "engine/classifier/__init__.py"
      provides: "Re-exports TransactionClassifier, AI_CONFIDENCE_THRESHOLD, CLASSIFICATION_SYSTEM_PROMPT"
      contains: "from engine.classifier"
    - path: "engine/acb/__init__.py"
      provides: "Re-exports ACBPool, ACBEngine, resolve_token_symbol, normalize_timestamp"
      contains: "from engine.acb"
    - path: "db/models/__init__.py"
      provides: "Re-exports Base + all 22 model classes"
      contains: "from db.models"
  key_links:
    - from: "engine/classifier/__init__.py"
      to: "engine/classifier/near_classifier.py"
      via: "re-export import"
      pattern: "from engine\\.classifier\\.near_classifier import"
    - from: "engine/acb/__init__.py"
      to: "engine/acb/pool.py"
      via: "re-export import"
      pattern: "from engine\\.acb\\.pool import"
    - from: "db/models/__init__.py"
      to: "db/models/users.py"
      via: "re-export import"
      pattern: "from db\\.models\\.users import"
---

<objective>
Split three large modules (classifier.py 1246 lines, acb.py 857 lines, models.py 961 lines) into focused sub-packages while preserving all existing import paths via __init__.py re-exports.

Purpose: Reduce cognitive load per file, enable focused editing, improve code navigation. No behavioral changes.
Output: Three new sub-packages (engine/classifier/, engine/acb/, db/models/) with backward-compatible re-exports.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-concerns-remediation/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split engine/classifier.py into sub-package</name>
  <files>engine/classifier/__init__.py, engine/classifier/near_classifier.py, engine/classifier/evm_classifier.py, engine/classifier/exchange_classifier.py, engine/classifier/writer.py</files>
  <action>
1. Read engine/classifier.py fully to understand class structure and internal method calls.
2. Create engine/classifier/ directory.
3. Move engine/classifier.py to engine/classifier/__init__.py via: `git mv engine/classifier.py engine/classifier_tmp.py && mkdir -p engine/classifier && mv engine/classifier_tmp.py engine/classifier/__init__.py`
4. Extract into sub-modules:
   - near_classifier.py: _classify_near_tx, _find_staking_event, _find_lockup_event, _load_staking_event_index, _load_lockup_event_index and related NEAR-specific helpers
   - evm_classifier.py: _classify_evm_tx_group and related EVM helpers
   - exchange_classifier.py: _classify_exchange_tx and related exchange helpers
   - writer.py: _write_records, _upsert_classification, _write_audit_log and DB persistence helpers
5. Update __init__.py to be a thin facade that imports TransactionClassifier (which lives in __init__.py itself or a core.py) and re-exports all public names: TransactionClassifier, AI_CONFIDENCE_THRESHOLD, CLASSIFICATION_SYSTEM_PROMPT, REVIEW_THRESHOLD.
6. The TransactionClassifier class should remain in __init__.py but delegate to imported functions from sub-modules. Each sub-module function should accept the classifier instance (self) as first arg or be standalone functions that take the needed params.
7. CRITICAL: Every `from engine.classifier import X` in the codebase must continue to work. Grep for all imports: `grep -rn "from engine.classifier import\|from engine import classifier\|import engine.classifier" --include="*.py"` and verify each one resolves.
8. Run `python -m pytest tests/ -x -q` to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -c "from engine.classifier import TransactionClassifier, AI_CONFIDENCE_THRESHOLD, REVIEW_THRESHOLD" && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>engine/classifier/ is a package with 4 sub-modules, no file exceeds 400 lines, all existing imports work, full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Split engine/acb.py and db/models.py into sub-packages</name>
  <files>engine/acb/__init__.py, engine/acb/pool.py, engine/acb/engine.py, engine/acb/symbols.py, db/models/__init__.py, db/models/users.py, db/models/wallets.py, db/models/events.py, db/models/prices.py, db/models/classification.py, db/models/acb.py, db/models/verification.py</files>
  <action>
1. Split engine/acb.py:
   - `git mv engine/acb.py engine/acb_tmp.py && mkdir -p engine/acb && mv engine/acb_tmp.py engine/acb/__init__.py`
   - Extract pool.py: ACBPool class only (~200 lines)
   - Extract engine.py: ACBEngine class only (~400 lines)
   - Extract symbols.py: TOKEN_SYMBOL_MAP, resolve_token_symbol(), normalize_timestamp() utilities
   - __init__.py re-exports: ACBPool, ACBEngine, resolve_token_symbol, normalize_timestamp
   - CRITICAL: engine/acb/engine.py has a lazy import of GainsCalculator — keep that pattern intact.

2. Split db/models.py:
   - `git mv db/models.py db/models_tmp.py && mkdir -p db/models && mv db/models_tmp.py db/models/__init__.py`
   - Extract users.py: User, Passkey, Session, Challenge, MagicLinkToken, AccountantAccess (auth-related models)
   - Extract wallets.py: Wallet, Transaction, IndexingJob (core data models)
   - Extract events.py: StakingEvent, EpochSnapshot, LockupEvent
   - Extract prices.py: PriceCache, PriceCacheMinute
   - Extract classification.py: ClassificationRule, TransactionClassification, SpamRule, ClassificationAuditLog
   - Extract acb.py: ACBSnapshot, CapitalGainsLedger, IncomeLedger
   - Extract verification.py: VerificationResult, AccountVerificationStatus
   - __init__.py: Import Base from one sub-module (users.py has it), then re-export Base + all 22 model classes
   - IMPORTANT: All models share the same `Base = declarative_base()`. Define Base in __init__.py or a dedicated base.py, then import it in each sub-module.

3. Verify all imports:
   - `grep -rn "from db.models import\|from db import models\|import db.models" --include="*.py"` — every one must still work
   - `grep -rn "from engine.acb import\|from engine import acb\|import engine.acb" --include="*.py"` — every one must still work
   - `python -c "from engine.acb import ACBPool, ACBEngine, resolve_token_symbol, normalize_timestamp; from db.models import User, Transaction, Wallet, Base"`

4. Run full test suite: `python -m pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -c "from engine.acb import ACBPool, ACBEngine, resolve_token_symbol, normalize_timestamp; from db.models import User, Transaction, Wallet, Base, PriceCache, ClassificationRule, ACBSnapshot, VerificationResult" && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>engine/acb/ is a package with 3 sub-modules, db/models/ is a package with 7 sub-modules, no file exceeds 400 lines, all existing imports work, full test suite passes.</done>
</task>

</tasks>

<verification>
1. Import smoke test: `python -c "from engine.classifier import TransactionClassifier; from engine.acb import ACBPool, ACBEngine; from db.models import User, Transaction, Wallet, Base"`
2. Line counts: No sub-module exceeds 400 lines: `find engine/classifier engine/acb db/models -name "*.py" -exec wc -l {} + | sort -n`
3. Full test suite: `python -m pytest tests/ -q` — all 400+ tests pass
4. No broken imports: `python -c "import importlib, pathlib; [importlib.import_module(str(p).replace('/','.').replace('.py','')) for p in pathlib.Path('.').glob('**/*.py') if '__pycache__' not in str(p)]"` — no ImportError
</verification>

<success_criteria>
- All three large modules split into sub-packages
- Every existing import path works without modification to callers
- No sub-module file exceeds 400 lines
- Full test suite (400+ tests) passes green
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-concerns-remediation/10-01-SUMMARY.md`
</output>
