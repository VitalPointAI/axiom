---
phase: 10-remaining-concerns-remediation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/capital_gains.py
  - reports/ledger.py
  - reports/export.py
  - indexers/staking_fetcher.py
  - indexers/nearblocks_client.py
autonomous: true
requirements: [RC-03, RC-04, RC-05]

must_haves:
  truths:
    - "Capital gains CSV export uses named cursor streaming instead of fetchall"
    - "Ledger report uses named cursor streaming instead of fetchall"
    - "Staking backfill commits in batches of 100 epochs instead of one-by-one"
    - "NearBlocks client caches repeated API responses with 5-minute TTL"
  artifacts:
    - path: "reports/capital_gains.py"
      provides: "Streaming CSV generation via named cursor"
      contains: "conn.cursor(name="
    - path: "indexers/staking_fetcher.py"
      provides: "Batched epoch backfill with periodic commits"
      contains: "BACKFILL_BATCH_SIZE"
    - path: "indexers/nearblocks_client.py"
      provides: "TTL cache for repeated API calls"
      contains: "_cache_get"
  key_links:
    - from: "reports/capital_gains.py"
      to: "psycopg2 named cursor"
      via: "server-side cursor iteration"
      pattern: "cursor\\(name="
    - from: "indexers/nearblocks_client.py"
      to: "_cache dict"
      via: "TTL-based cache lookup before API call"
      pattern: "_cache_get|_cache_set"
---

<objective>
Implement streaming CSV export for large reports (named cursors), batch epoch backfill commits, and NearBlocks API response caching.

Purpose: Reduce memory usage for large report generation, reduce DB transaction overhead for staking backfill, reduce redundant API calls.
Output: Updated report modules with streaming, updated staking_fetcher with batching, updated nearblocks_client with TTL cache.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-concerns-remediation/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Streaming CSV export with named cursors</name>
  <files>reports/capital_gains.py, reports/ledger.py, reports/export.py, tests/test_reports.py</files>
  <behavior>
    - Test: streaming capital gains CSV uses named cursor (mock cursor creation, verify name= arg)
    - Test: streaming ledger uses named cursor
    - Test: named cursor is closed before putconn (verify close() called before putconn())
  </behavior>
  <action>
1. Read reports/capital_gains.py, reports/ledger.py, reports/export.py to find all fetchall() calls on large queries.
2. For capital_gains.py:
   - Replace fetchall() in generate_chronological_csv and generate_grouped_csv with named cursor pattern
   - Use `conn.cursor(name="capital_gains_stream")` with `itersize = 1000`
   - Iterate rows with `for row in cur:` instead of loading all into list
   - ALWAYS close named cursor in finally block before putconn()
   - Keep small queries (like summary totals) as regular fetchall — only stream the detail rows
3. For ledger.py:
   - Replace the large UNION ALL query fetchall with named cursor streaming
   - Use `conn.cursor(name="ledger_stream")` with `itersize = 1000`
4. For export.py (KoinlyExport, AccountingExporter):
   - Replace fetchall in Koinly and accounting export queries with named cursor
   - Use `conn.cursor(name="export_stream")` with `itersize = 500`
5. Add tests to tests/test_reports.py:
   - test_streaming_capital_gains_uses_named_cursor: Mock pool, verify cursor created with name= kwarg
   - test_streaming_ledger_uses_named_cursor: Same pattern
   - test_named_cursor_closed_before_putconn: Mock pool, verify cur.close() called
6. Run: `python -m pytest tests/test_reports.py -x -q`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_reports.py -x -q</automated>
  </verify>
  <done>Capital gains, ledger, and export reports use named cursor streaming for large queries. Named cursors properly closed in finally blocks. Tests verify cursor creation pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill batch commits + NearBlocks API caching</name>
  <files>indexers/staking_fetcher.py, indexers/nearblocks_client.py, tests/test_near_fetcher.py</files>
  <behavior>
    - Test: backfill commits every BACKFILL_BATCH_SIZE epochs (verify conn.commit count)
    - Test: NearBlocks cache hit returns cached value without API call
    - Test: NearBlocks cache miss after TTL expiry makes fresh API call
  </behavior>
  <action>
1. Read indexers/staking_fetcher.py — find backfill_epoch_rewards and its commit pattern.
2. Add BACKFILL_BATCH_SIZE = 100 constant.
3. Refactor backfill loop to accumulate reward records and flush every BACKFILL_BATCH_SIZE:
   - Collect epoch results in a batch list
   - When batch reaches BACKFILL_BATCH_SIZE, execute batch INSERT (execute_values or executemany) and conn.commit()
   - Update cursor on indexing_job after each batch commit (enables resume on failure)
   - Flush remaining batch at end

4. Read indexers/nearblocks_client.py — add TTL cache:
   - Add `_CACHE_TTL = 300` (5 min) class constant
   - Add `self._cache: dict[str, tuple] = {}` in __init__
   - Add `_cache_get(key)` and `_cache_set(key, value)` methods
   - Wrap get_transaction_count() with cache: check cache first, API call on miss, cache result
   - Also cache get_account_txns_count() if it exists

5. Add tests to tests/test_near_fetcher.py:
   - test_backfill_batch_commits: Mock conn, run backfill with 250 epochs, verify conn.commit called 3 times (100+100+50)
   - test_nearblocks_cache_hit: Set cache entry, call get_transaction_count, verify no _request() call
   - test_nearblocks_cache_expired: Set cache with expired TTL, call method, verify _request() called
6. Run: `python -m pytest tests/test_near_fetcher.py -x -q && python -m pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_near_fetcher.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Staking backfill commits every 100 epochs with resume support. NearBlocks client caches repeated API calls with 5-min TTL. Tests verify batching and caching behavior.</done>
</task>

</tasks>

<verification>
1. Streaming: `grep -n "cursor(name=" reports/capital_gains.py reports/ledger.py reports/export.py` — should show named cursor usage
2. Batching: `grep -n "BACKFILL_BATCH_SIZE" indexers/staking_fetcher.py` — should show batch constant
3. Caching: `grep -n "_cache" indexers/nearblocks_client.py` — should show cache methods
4. Full suite: `python -m pytest tests/ -q`
</verification>

<success_criteria>
- Report CSV generation streams via named cursors (no fetchall for large queries)
- Named cursors properly closed before putconn in all code paths
- Staking backfill commits every 100 epochs with cursor update
- NearBlocks API responses cached with 5-minute TTL
- All tests pass including new streaming/batching/caching tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-concerns-remediation/10-03-SUMMARY.md`
</output>
