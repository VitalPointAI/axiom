---
phase: 10-remaining-concerns-remediation
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - tests/test_classifier.py
  - tests/test_acb.py
autonomous: true
requirements: [RC-10, RC-11, RC-12]

must_haves:
  truths:
    - "Classification rule priority resolution is tested (higher priority wins)"
    - "Equal priority tie-breaking is tested (first in sorted list wins)"
    - "Chain filter prevents cross-chain rule application"
    - "ACB handles missing price gracefully (skip or flag, no crash)"
    - "ACB handles zero-holdings disposal (oversell with needs_review)"
    - "Concurrent upsert preserves specialist_confirmed rows"
    - "Duplicate classify call produces idempotent results"
  artifacts:
    - path: "tests/test_classifier.py"
      provides: "Rule priority, conflict, chain filter, and idempotency tests"
      contains: "test_higher_priority_rule_wins"
    - path: "tests/test_acb.py"
      provides: "Missing price, None amount, and oversell tests"
      contains: "test_missing_price"
  key_links:
    - from: "tests/test_classifier.py"
      to: "engine/classifier"
      via: "import TransactionClassifier"
      pattern: "from engine.classifier import"
    - from: "tests/test_acb.py"
      to: "engine/acb"
      via: "import ACBPool, ACBEngine"
      pattern: "from engine.acb import"
---

<objective>
Add test coverage for classification rule interactions (priority resolution, conflicts, chain filters), ACB gap data handling (missing prices, None amounts, oversell), and concurrent classification idempotency.

Purpose: Cover the three remaining test gaps identified in CONCERNS.md — ensure rule priority, ACB edge cases, and concurrent upsert behavior are validated.
Output: ~15 new tests across test_classifier.py and test_acb.py.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-concerns-remediation/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classification rule interaction + concurrent upsert tests</name>
  <files>tests/test_classifier.py</files>
  <behavior>
    - test_higher_priority_rule_wins_over_lower: Two rules matching same tx pattern, different priorities; higher priority classification applied
    - test_equal_priority_first_rule_wins: Two rules at same priority, first in sorted order wins (stable sort)
    - test_conflicting_categories_resolved_by_priority: Staking rule at priority 100 vs transfer at 50; staking category wins
    - test_chain_filter_prevents_wrong_chain_rule: NEAR-only rule does not match EVM tx even if method_name matches
    - test_no_match_falls_through_to_unknown: Transaction matching zero rules gets "unknown" category
    - test_concurrent_upsert_preserves_specialist_confirmed: Upsert on specialist_confirmed=True row does not overwrite
    - test_duplicate_classify_call_idempotent: Calling classify twice produces same classification result
  </behavior>
  <action>
1. Read tests/test_classifier.py to understand existing test patterns, fixtures, and mock setup.
2. Add new test functions following the existing mock pool pattern (no real DB needed):

For rule priority tests:
- Create two mock rules with same pattern but different priorities (e.g., priority 100 "staking_reward" and priority 50 "transfer")
- Build a NEAR tx dict that matches both rules' patterns
- Call classifier._match_rules() or the full classify path
- Assert the higher priority rule's category is applied

For chain filter test:
- Create a NEAR-specific rule (chain="near")
- Build an EVM tx dict with matching method_name
- Assert the NEAR rule does NOT match the EVM tx

For no-match test:
- Create a tx that matches no existing rules
- Assert classification is "unknown" or falls through to AI fallback

For concurrent upsert tests:
- Mock the cursor to simulate an existing classification with specialist_confirmed=True
- Call _upsert_classification with a different category
- Verify the SQL includes `WHERE NOT specialist_confirmed` (check cursor.execute args)
- For idempotency: mock fetchall to return same tx twice, classify both, verify single classification result

3. Run: `python -m pytest tests/test_classifier.py -x -q`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_classifier.py -k "priority or chain_filter or unknown or specialist or idempotent" -x -q</automated>
  </verify>
  <done>7 new classifier tests covering rule priority resolution, chain filtering, unknown fallthrough, specialist_confirmed preservation, and classify idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ACB gap data and edge case tests</name>
  <files>tests/test_acb.py</files>
  <behavior>
    - test_missing_price_skips_income_row: price_service returns None for get_price_cad_at_timestamp; income row is skipped or flagged needs_review, no crash
    - test_none_amount_transaction_handled: Transaction with amount=None does not raise; ACBPool state unchanged
    - test_disposal_with_no_price_uses_estimate: Disposal with estimated price sets is_estimated=True
    - test_oversell_zero_holdings: Disposal after all units sold records oversell with needs_review=True
  </behavior>
  <action>
1. Read tests/test_acb.py to understand existing test patterns, ACBPool mock setup, and GainsCalculator mock patterns.
2. Add new test functions:

For missing price test:
- Mock price_service.get_price_cad_at_timestamp to return None
- Create an income transaction (e.g., staking reward)
- Call ACBEngine.replay or the relevant method
- Assert no crash; verify the income row is either skipped or recorded with needs_review=True

For None amount test:
- Create a transaction dict with amount=None
- Call ACBPool.acquire() or ACBEngine processing
- Assert no exception raised; pool state unchanged (units_held stays same)

For estimated price test:
- Mock price_service to return (price, is_estimated=True) or similar
- Create a disposal transaction
- Verify GainsCalculator records the disposal with is_estimated flag

For oversell test:
- Create an ACBPool with 10 units
- Dispose 15 units (oversell by 5)
- Assert pool.needs_review is True
- Assert pool.units_held is 0 (clamped, not negative)
- Verify snapshot has needs_review=True

3. Run: `python -m pytest tests/test_acb.py -x -q`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_acb.py -k "missing_price or none_amount or estimate or oversell" -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>4 new ACB tests covering missing price, None amount, estimated price, and oversell scenarios. All pass alongside existing test suite.</done>
</task>

</tasks>

<verification>
1. New test count: `python -m pytest tests/test_classifier.py tests/test_acb.py --co -q | wc -l` — should show ~15+ more tests than before
2. Targeted run: `python -m pytest tests/test_classifier.py tests/test_acb.py -x -q` — all pass
3. Full suite: `python -m pytest tests/ -q` — all 400+ tests pass
</verification>

<success_criteria>
- ~7 new classifier tests for rule priority, chain filter, unknown fallthrough, specialist preserve, idempotency
- ~4 new ACB tests for missing price, None amount, estimated price, oversell
- All new tests pass alongside existing 400+ test suite
- No test relies on real DB or external API calls
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-concerns-remediation/10-05-SUMMARY.md`
</output>
