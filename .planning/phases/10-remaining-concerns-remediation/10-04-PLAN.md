---
phase: 10-remaining-concerns-remediation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - indexers/xrp_fetcher.py
  - indexers/akash_fetcher.py
  - indexers/coinbase_pro_indexer.py
  - api/routers/portfolio.py
  - docs/EXCHANGE_IMPORT_DESIGN.md
  - docs/INDEXER_RULES.md
  - docs/STUB_IMPLEMENTATIONS.md
  - docs/LOGGING_POLICY.md
autonomous: true
requirements: [RC-07, RC-08, RC-13, RC-14]

must_haves:
  truths:
    - "sanitize_for_log() redacts sensitive keys (DATABASE_URL, API_KEY, SECRET, TOKEN, PASSWORD)"
    - "xrp_fetcher and akash_fetcher log a STUB warning on initialization"
    - "coinbase_pro_indexer.py emits DeprecationWarning on import"
    - "No SQLite references remain in docs/ markdown files"
  artifacts:
    - path: "config.py"
      provides: "sanitize_for_log() helper and _SENSITIVE_PATTERNS list"
      contains: "sanitize_for_log"
    - path: "docs/STUB_IMPLEMENTATIONS.md"
      provides: "Documentation of stub status for xrp_fetcher, akash_fetcher, portfolio"
      contains: "STUB"
    - path: "docs/LOGGING_POLICY.md"
      provides: "Policy document for what must never be logged"
      contains: "SENSITIVE"
  key_links:
    - from: "config.py"
      to: "logging callers"
      via: "sanitize_for_log import"
      pattern: "sanitize_for_log"
    - from: "indexers/coinbase_pro_indexer.py"
      to: "warnings module"
      via: "DeprecationWarning"
      pattern: "warnings\\.warn"
---

<objective>
Add logging sanitization helper, document/mark stub implementations, add deprecation warning to coinbase_pro_indexer, and remove SQLite references from docs.

Purpose: Prevent sensitive data leakage in logs, clarify stub status for consumers, signal deprecated code paths, keep documentation accurate.
Output: sanitize_for_log() in config.py, stub docs, deprecation warning, updated docs.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-concerns-remediation/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Logging sanitization + stub documentation + deprecation</name>
  <files>config.py, tests/test_config_validation.py</files>
  <behavior>
    - Test: sanitize_for_log({"DATABASE_URL": "postgres://..."}) returns {"DATABASE_URL": "***REDACTED***"}
    - Test: sanitize_for_log({"name": "Aaron"}) returns {"name": "Aaron"} (non-sensitive preserved)
    - Test: sanitize_for_log handles NEARBLOCKS_API_KEY, SECRET_KEY, SESSION_TOKEN
  </behavior>
  <action>
1. Add to config.py:
   - `_SENSITIVE_PATTERNS = ["DATABASE_URL", "API_KEY", "SECRET", "TOKEN", "PASSWORD", "CREDENTIAL"]`
   - `def sanitize_for_log(data: dict) -> dict:` — returns copy with sensitive fields redacted to "***REDACTED***"
   - Match is case-insensitive: `any(pat in k.upper() for pat in _SENSITIVE_PATTERNS)`

2. Add tests to tests/test_config_validation.py:
   - test_sanitize_redacts_database_url
   - test_sanitize_redacts_api_key_variants (NEARBLOCKS_API_KEY, COINGECKO_API_KEY)
   - test_sanitize_preserves_safe_keys (name, email, user_id)
   - test_sanitize_redacts_token_and_secret (SESSION_TOKEN, JWT_SECRET)

3. Run: `python -m pytest tests/test_config_validation.py -k sanitize -x -q`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_config_validation.py -k sanitize -x -q</automated>
  </verify>
  <done>sanitize_for_log() exists in config.py, redacts all sensitive patterns, tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Stub warnings + deprecation + docs cleanup</name>
  <files>indexers/xrp_fetcher.py, indexers/akash_fetcher.py, indexers/coinbase_pro_indexer.py, api/routers/portfolio.py, docs/EXCHANGE_IMPORT_DESIGN.md, docs/INDEXER_RULES.md, docs/STUB_IMPLEMENTATIONS.md, docs/LOGGING_POLICY.md, tests/test_coinbase_pro_deprecation.py</files>
  <action>
1. Update indexers/xrp_fetcher.py:
   - Add module-level docstring update: "# STUB: Untested against live XRPL API. Parses transactions correctly but has not been validated with real API responses."
   - In __init__, add `logger.warning("XRPFetcher is a STUB implementation — untested against live XRPL API")`

2. Update indexers/akash_fetcher.py:
   - Same pattern: docstring update + logger.warning in __init__

3. Update api/routers/portfolio.py:
   - Add OpenAPI description to the stub endpoint: `summary="Portfolio summary (stub)", description="Not yet implemented. Returns 404."`

4. Update indexers/coinbase_pro_indexer.py:
   - Add deprecation warning at module level (after imports):
     ```python
     import warnings
     warnings.warn(
         "coinbase_pro_indexer.py is deprecated. "
         "Use indexers/exchange_parsers/coinbase.py for Coinbase CSV imports. "
         "Will be removed in v2.",
         DeprecationWarning,
         stacklevel=2,
     )
     ```
   - Add header comment: `# DEPRECATED: Use indexers/exchange_parsers/coinbase.py instead.`

5. Create docs/STUB_IMPLEMENTATIONS.md:
   - List: xrp_fetcher.py (functional stub, untested live), akash_fetcher.py (functional stub, untested live), portfolio GET / (explicit 404 stub)
   - For each: status, what works, what's missing, migration path

6. Create docs/LOGGING_POLICY.md:
   - List all sensitive fields that must never appear in logs
   - Reference sanitize_for_log() in config.py
   - Document which log levels are safe for production

7. Update docs/EXCHANGE_IMPORT_DESIGN.md and docs/INDEXER_RULES.md:
   - `grep -i sqlite` in each file
   - Replace SQLite references with PostgreSQL equivalents
   - Update scaling limits to reflect PostgreSQL capabilities
   - If no SQLite references found, note that docs are already clean

8. Create tests/test_coinbase_pro_deprecation.py:
   - test_coinbase_pro_emits_deprecation_warning: `with warnings.catch_warnings(record=True) as w: warnings.simplefilter("always"); import importlib; importlib.reload(coinbase_pro_indexer); assert any(issubclass(warning.category, DeprecationWarning) for warning in w)`

9. Run: `python -m pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_coinbase_pro_deprecation.py -x -q && grep -ri sqlite docs/*.md; echo "EXIT:$?"</automated>
  </verify>
  <done>Stubs log warnings on init, coinbase_pro_indexer emits DeprecationWarning, docs have no SQLite references, STUB_IMPLEMENTATIONS.md and LOGGING_POLICY.md created.</done>
</task>

</tasks>

<verification>
1. Stub warnings: `python -c "import logging; logging.basicConfig(level='WARNING'); from indexers.xrp_fetcher import XRPFetcher"` — should show STUB warning
2. Deprecation: `python -W all -c "import indexers.coinbase_pro_indexer"` — should show DeprecationWarning
3. SQLite check: `grep -ri sqlite docs/*.md` — should return no results
4. Policy docs: `ls docs/STUB_IMPLEMENTATIONS.md docs/LOGGING_POLICY.md` — both exist
5. Full suite: `python -m pytest tests/ -q`
</verification>

<success_criteria>
- sanitize_for_log() redacts all sensitive patterns with tests
- xrp_fetcher/akash_fetcher log STUB warnings on initialization
- coinbase_pro_indexer emits DeprecationWarning on import
- Portfolio stub has OpenAPI description
- docs/ contains no SQLite references
- STUB_IMPLEMENTATIONS.md and LOGGING_POLICY.md exist
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-concerns-remediation/10-04-SUMMARY.md`
</output>
