---
phase: 09-code-quality-hardening
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - verify/reconcile.py
  - verify/diagnosis.py
autonomous: true
requirements: [QH-02]

must_haves:
  truths:
    - "Reconcile module diagnosis logic is extracted into a separate focused module"
    - "verify/reconcile.py is under 600 lines after extraction"
    - "All existing verification behavior is preserved (no functional changes)"
  artifacts:
    - path: "verify/diagnosis.py"
      provides: "Extracted diagnosis helpers from reconcile.py"
      contains: "_diagnose_missing_staking"
    - path: "verify/reconcile.py"
      provides: "Slimmed reconcile module delegating to diagnosis.py"
      contains: "from verify.diagnosis import"
  key_links:
    - from: "verify/reconcile.py"
      to: "verify/diagnosis.py"
      via: "import of diagnosis functions"
      pattern: "from verify.diagnosis import"
---

<objective>
Extract diagnosis helpers from verify/reconcile.py to reduce its 1002-line monolith.

Purpose: QH-02 requires refactoring both classifier (done in 09-03) and reconcile. The reconcile module has 4 diagnosis methods (~300 lines total) plus the _auto_diagnose dispatcher that form a natural extraction boundary.
Output: New verify/diagnosis.py with extracted diagnosis logic; reconcile.py slimmed to under 600 lines.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-code-quality-hardening/09-RESEARCH.md
@verify/reconcile.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract diagnosis helpers from reconcile.py into verify/diagnosis.py</name>
  <files>verify/reconcile.py, verify/diagnosis.py</files>
  <action>
1. Read `verify/reconcile.py` fully. Identify the diagnosis cluster:
   - `_auto_diagnose()` (~line 629) — dispatcher that calls the 4 diagnosis methods below
   - `_diagnose_missing_staking()` (~line 679)
   - `_diagnose_uncounted_fees()` (~line 738)
   - `_diagnose_unindexed_period()` (~line 802)
   - `_diagnose_classification_error()` (~line 853)

   These 5 methods together span ~280 lines and form a cohesive "diagnosis" concern separate from the core reconciliation logic.

2. **Create `verify/diagnosis.py`**:
   - Move the 5 diagnosis methods into a new `ReconcileDiagnoser` class (or standalone functions — match the project's style)
   - The diagnosis methods need access to: `self.pool` (database connection pool), `logger`
   - If using a class: `class ReconcileDiagnoser:` with `__init__(self, pool)` taking the connection pool
   - If using functions: each function takes `pool` (or `conn`) as first parameter
   - Preserve ALL existing logic exactly — this is a move, not a rewrite
   - Add module docstring: "Diagnosis helpers for balance reconciliation. Extracted from reconcile.py for maintainability."

3. **Update `verify/reconcile.py`**:
   - Add `from verify.diagnosis import ReconcileDiagnoser` (or individual function imports)
   - In `BalanceReconciler.__init__()`, create `self.diagnoser = ReconcileDiagnoser(self.pool)`
   - Replace calls to `self._auto_diagnose(...)` with `self.diagnoser.auto_diagnose(...)`
   - Remove the 5 moved methods from reconcile.py
   - Keep all other methods (reconcile_user, _reconcile_wallet, _get_*_balance, _upsert_result, etc.) in place

4. Verify line count: `wc -l verify/reconcile.py` should be under 600 lines.

5. Run any existing tests that exercise verification:
   `pytest tests/ -q --tb=short -x -k "verif or reconcil"`
   Then run the full suite: `pytest tests/ -q --tb=short -x`

IMPORTANT: This is a pure extraction refactor. Do NOT change any logic, SQL queries, or return values. The only change is where the code lives.
  </action>
  <verify>
    <automated>python -c "from verify.diagnosis import ReconcileDiagnoser; print('diagnosis module OK')" && python -c "from verify.reconcile import BalanceReconciler; print('reconcile import OK')" && test $(wc -l < verify/reconcile.py) -lt 700 && echo "line count OK" && pytest tests/ -q --tb=short -x</automated>
  </verify>
  <done>verify/diagnosis.py exists with extracted diagnosis methods. verify/reconcile.py imports from it and is under 600 lines. All existing tests pass unchanged.</done>
</task>

</tasks>

<verification>
- `from verify.diagnosis import ReconcileDiagnoser` succeeds
- `from verify.reconcile import BalanceReconciler` succeeds (no import errors)
- `wc -l verify/reconcile.py` shows under 700 lines
- `wc -l verify/diagnosis.py` shows ~280+ lines
- `pytest tests/ -q --tb=short` all pass
</verification>

<success_criteria>
- Diagnosis logic extracted into verify/diagnosis.py as a cohesive module
- reconcile.py reduced from 1002 lines to under 600
- Zero functional changes — all reconciliation behavior identical
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-code-quality-hardening/09-05-SUMMARY.md`
</output>
