---
phase: 07-web-ui
plan: 04
wave: 3
depends_on: [07-02, 07-03]
autonomous: true
requirements: [UI-04, UI-05]
files_modified:
  - web/app/transactions/page.tsx
  - web/components/transaction-table.tsx
  - web/components/transaction-filters.tsx
  - web/components/transaction-detail-sheet.tsx
  - web/app/api/transactions/route.ts
  - web/app/api/transactions/[id]/route.ts
must_haves:
  truths:
    - Transaction ledger displays all user transactions
    - Filters work for date, type, asset, and amount
    - Search finds transactions by hash or memo
    - Transaction details can be viewed and edited
  artifacts:
    - path: web/app/transactions/page.tsx
      provides: Transaction ledger view
    - path: web/app/api/transactions/route.ts
      provides: Transaction query API with filters
---

<objective>
Build the transaction ledger with filtering, search, pagination, and detail editing capabilities.
</objective>

<context>
Transactions table has:
- tx_hash, timestamp, type (transfer, stake, swap, etc.)
- from_address, to_address
- asset, amount, fee
- classification (income, gain, loss, transfer)
- notes (user-editable)

Need efficient pagination for potentially thousands of transactions.
</context>

<tasks>
  <task type="auto">
    <name>Create transaction table</name>
    <files>web/app/transactions/page.tsx, web/components/transaction-table.tsx</files>
    <action>
    Build transaction ledger:
    1. Table with columns: Date, Type, Asset, Amount, From/To, Classification
    2. Sortable columns (click header)
    3. Pagination (25/50/100 per page)
    4. Click row to open detail sheet
    
    Use shadcn/ui Table component.
    Format amounts with proper decimals.
    Color-code types (green=income, red=expense, gray=transfer).
    </action>
    <verify>Table displays transactions with pagination</verify>
    <done>Transaction table renders correctly</done>
  </task>

  <task type="auto">
    <name>Build filters component</name>
    <files>web/components/transaction-filters.tsx, web/app/api/transactions/route.ts</files>
    <action>
    Create TransactionFilters with:
    - Date range picker (from/to)
    - Type multi-select (transfer, stake, unstake, swap, etc.)
    - Asset selector (NEAR, ETH, etc.)
    - Amount range (min/max)
    - Search box (hash, memo, address)
    - Clear all filters button
    
    API supports query params:
    GET /api/transactions?from=2025-01-01&to=2025-12-31&type=stake&asset=NEAR&q=search
    
    Filters update URL for shareable links.
    </action>
    <verify>Applying filters updates table results</verify>
    <done>Filtering works correctly</done>
  </task>

  <task type="auto">
    <name>Build transaction detail sheet</name>
    <files>web/components/transaction-detail-sheet.tsx</files>
    <action>
    Create slide-out sheet showing full transaction details:
    - All transaction fields (hash, timestamp, etc.)
    - Link to block explorer
    - Classification dropdown (editable)
    - Notes textarea (editable)
    - Related transactions (if part of swap)
    - Save button for changes
    
    Use shadcn/ui Sheet component.
    </action>
    <verify>Can view and edit transaction details</verify>
    <done>Detail sheet working</done>
  </task>

  <task type="auto">
    <name>Implement transaction editing</name>
    <files>web/app/api/transactions/[id]/route.ts</files>
    <action>
    Allow users to modify:
    - classification (override auto-classification)
    - notes (free text for accountant)
    
    PATCH /api/transactions/[id]
    ```typescript
    { classification?: string, notes?: string }
    ```
    
    Track if classification was manually overridden.
    Show indicator on overridden transactions.
    </action>
    <verify>Edits persist and show in table</verify>
    <done>Transaction editing saves correctly</done>
  </task>
</tasks>

<verification>
1. /transactions page shows paginated ledger
2. Can filter by date range
3. Can filter by transaction type
4. Can filter by asset
5. Search finds by hash or memo
6. Clicking transaction opens detail sheet
7. Can edit classification and notes
8. Edits persist after page reload
9. Pagination works with filters applied
</verification>
