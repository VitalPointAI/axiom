---
phase: 07-web-ui
plan: 04
type: execute
wave: 3
depends_on: ["07-01"]
files_modified:
  - api/routers/transactions.py
  - api/schemas/transactions.py
  - tests/test_api_transactions.py
autonomous: true
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - "User can view paginated transaction ledger filtered by date, type, asset, amount, chain"
    - "User can edit a transaction's classification (change tax_category, add notes, mark reviewed)"
    - "User can view a review queue of all needs_review flagged transactions"
    - "Classification edits trigger ACB recalculation for affected tokens"
  artifacts:
    - path: "api/routers/transactions.py"
      provides: "Transaction ledger + classification editing endpoints"
      min_lines: 120
    - path: "api/schemas/transactions.py"
      provides: "Transaction response + classification update schemas"
      exports: ["TransactionResponse", "ClassificationUpdate", "ReviewQueueResponse"]
  key_links:
    - from: "api/routers/transactions.py"
      to: "transaction_classifications table"
      via: "UPDATE tax_category + notes on PATCH"
      pattern: "UPDATE transaction_classifications"
    - from: "api/routers/transactions.py"
      to: "indexing_jobs table"
      via: "INSERT calculate_acb job on apply-changes"
      pattern: "calculate_acb"
---

<objective>
Build the transaction ledger API with filtering, pagination, search, classification editing, and a review queue for needs_review items. Classification edits stage changes and "Apply Changes" triggers ACB recalculation.

Purpose: The transaction ledger is the core data view. Users and accountants need to filter, search, review flagged items, and correct classifications before generating reports.
Output: Transaction ledger API + classification editing + review queue + batch recalc trigger.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-web-ui/07-CONTEXT.md
@.planning/phases/07-web-ui/07-RESEARCH.md
@.planning/phases/07-web-ui/07-01-SUMMARY.md

<interfaces>
From api/dependencies.py (Plan 01):
```python
async def get_effective_user(...) -> dict
def get_db_conn(pool) -> Connection
```

From db schema (transaction_classifications - migration 003):
```sql
CREATE TABLE transaction_classifications (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL,
    tx_hash TEXT NOT NULL,
    chain TEXT NOT NULL DEFAULT 'NEAR',
    tax_category TEXT NOT NULL,
    sub_category TEXT,
    confidence_score NUMERIC(4,3),
    classification_source TEXT DEFAULT 'rule',
    rule_id INTEGER REFERENCES classification_rules(id),
    needs_review BOOLEAN DEFAULT FALSE,
    reviewed_at TIMESTAMPTZ,
    reviewer_notes TEXT,
    parent_classification_id INTEGER REFERENCES transaction_classifications(id),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

From db schema (transactions - migration 001, NEAR):
```sql
-- tx_hash, block_timestamp, sender_id, receiver_id, amount, token, action_type, ...
```

From db schema (exchange_transactions - migration 002):
```sql
-- tx_id, exchange, tx_date, tx_type, asset, quantity, total_price, fee, ...
```

LedgerReport UNION ALL pattern (from Phase 6 reports/ledger.py):
- UNION ALL across NEAR transactions + EVM transactions + exchange_transactions
- Joined with transaction_classifications for tax_category
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transaction ledger with filtering, pagination, and search</name>
  <files>api/routers/transactions.py, api/schemas/transactions.py, tests/test_api_transactions.py</files>
  <behavior>
    - test_list_transactions: GET /api/transactions returns paginated results with total count
    - test_filter_by_date: ?start_date=2025-01-01&end_date=2025-12-31 filters correctly
    - test_filter_by_type: ?tax_category=income returns only income transactions
    - test_filter_by_asset: ?asset=NEAR filters by token symbol
    - test_filter_by_chain: ?chain=NEAR filters by chain
    - test_filter_needs_review: ?needs_review=true returns flagged transactions
    - test_search: ?search=swap searches tx_hash, sender, receiver
    - test_pagination: ?page=2&per_page=50 returns correct page with offset
    - test_user_isolation: returns only effective_user's transactions
  </behavior>
  <action>
1. **`api/schemas/transactions.py`:**
   - TransactionResponse: id, tx_hash, chain, timestamp, sender, receiver, amount (str), token_symbol, action_type, tax_category, sub_category, confidence_score, needs_review, reviewer_notes, source ("on_chain"|"exchange")
   - TransactionListResponse: transactions (list), total (int), page (int), per_page (int), pages (int)
   - TransactionFilters: start_date, end_date, tax_category, asset, chain, needs_review (bool), search (str), page (int, default=1), per_page (int, default=50, max=200)

2. **`api/routers/transactions.py`:** GET /api/transactions:
   - Build a UNION ALL query across NEAR transactions, EVM transactions (if exists), and exchange_transactions — same pattern as LedgerReport in `reports/ledger.py`. LEFT JOIN with transaction_classifications for tax_category.
   - Apply filters via WHERE clauses: date range on block_timestamp/tx_date, tax_category match, token/asset ILIKE, chain match, needs_review boolean, search ILIKE on tx_hash/sender/receiver.
   - Paginate with LIMIT/OFFSET and return total count via COUNT(*) OVER().
   - Filter by user's wallet_ids (SELECT id FROM wallets WHERE user_id=effective_user_id) for on-chain transactions, and user_id for exchange transactions.
   - ORDER BY timestamp DESC (most recent first).

   Use `run_in_threadpool` for all DB calls.

3. **Tests:** Mock DB to return sample transaction rows. Test each filter individually and combined. Test pagination math. Test user isolation.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_api_transactions.py::TestTransactionList -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Transaction ledger returns paginated, filtered results from UNION ALL of on-chain + exchange transactions with classification data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classification editing + review queue + batch recalc</name>
  <files>api/routers/transactions.py, tests/test_api_transactions.py</files>
  <behavior>
    - test_patch_classification: PATCH /api/transactions/{id}/classification updates tax_category and reviewer_notes
    - test_patch_mark_reviewed: PATCH with needs_review=false sets reviewed_at timestamp
    - test_patch_unauthorized: cannot edit other user's transaction classification
    - test_review_queue: GET /api/transactions/review returns all needs_review=true items grouped by category
    - test_apply_changes: POST /api/transactions/apply-changes creates calculate_acb job for affected token symbols
    - test_apply_changes_tokens: apply-changes job cursor contains only edited token symbols
  </behavior>
  <action>
1. **Add to `api/schemas/transactions.py`:**
   - ClassificationUpdate: tax_category (optional str), sub_category (optional str), reviewer_notes (optional str), needs_review (optional bool)
   - ReviewQueueResponse: items (list[TransactionResponse]), counts_by_category (dict[str, int]), total (int)
   - ApplyChangesRequest: token_symbols (optional list[str]) — if not provided, recalc all tokens with recent edits

2. **Add endpoints to `api/routers/transactions.py`:**
   - PATCH /api/transactions/{tx_hash}/classification: Verify tx belongs to user (via wallet_id check). UPDATE transaction_classifications SET tax_category=..., reviewer_notes=..., needs_review=..., reviewed_at=NOW() (if needs_review=False), updated_at=NOW(). Track which token_symbols were edited (store in session or return list).
   - GET /api/transactions/review: SELECT from transaction_classifications tc JOIN transactions t WHERE tc.needs_review=true AND wallet_id IN (user's wallets). Group counts by tax_category. Order by confidence_score ASC (least confident first). Include same response shape as transaction list.
   - POST /api/transactions/apply-changes: Accept optional token_symbols list. If not provided, find all tokens with updated_at > last calculate_acb job completion. INSERT calculate_acb job into indexing_jobs with cursor containing affected token_symbols. This triggers ACBEngine recalc for those tokens only. Return job_id for polling.

3. **Tests:** Mock DB for classification updates. Test that PATCH updates correct fields. Test review queue filtering. Test that apply-changes creates correct job with token list.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_api_transactions.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Classification editing works with staged changes. Review queue shows all flagged items. Apply-changes triggers targeted ACB recalculation.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_api_transactions.py -x -q` all pass
- GET /api/transactions supports all filter types + pagination
- PATCH updates classification and sets reviewed_at
- POST apply-changes creates calculate_acb job
</verification>

<success_criteria>
- Transaction ledger queries across NEAR + EVM + exchange tables via UNION ALL
- Filters work: date range, tax_category, asset, chain, needs_review, search
- Classification edits are staged, apply-changes triggers recalc
- Review queue lists all needs_review items sorted by confidence
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-ui/07-04-SUMMARY.md`
</output>
