---
phase: 07-web-ui
plan: 07
type: execute
wave: 5
depends_on: ["07-06"]
files_modified:
  - api/Dockerfile
  - docker-compose.prod.yml
  - .github/workflows/deploy.yml
  - scripts/deploy.sh
  - web/app/api/
autonomous: false

requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08

must_haves:
  truths:
    - "FastAPI runs as its own Docker container alongside web, indexer, and postgres"
    - "Next.js API routes are removed (web/app/api/ deleted)"
    - "GitHub Actions deploy workflow builds and deploys the new api container"
    - "Health checks pass for all 4 services after deployment"
    - "End-to-end flow works: login -> add wallet -> see pipeline progress -> view reports"
  artifacts:
    - path: "api/Dockerfile"
      provides: "FastAPI Docker image"
      contains: "uvicorn api.main:app"
    - path: "docker-compose.prod.yml"
      provides: "Updated compose with api service"
      contains: "api:"
  key_links:
    - from: "docker-compose.prod.yml"
      to: "api/Dockerfile"
      via: "api service build"
      pattern: "dockerfile: api/Dockerfile"
    - from: ".github/workflows/deploy.yml"
      to: "docker-compose.prod.yml"
      via: "docker compose build and deploy"
      pattern: "docker.*compose"
---

<objective>
Finalize the deployment: create FastAPI Dockerfile, update docker-compose.prod.yml with the api service, update GitHub Actions deploy workflow, remove old Next.js API routes, and verify end-to-end.

Purpose: This is the integration wave that makes everything production-ready. The old Next.js API routes are deleted after confirming FastAPI handles all requests.
Output: Production-deployable stack with 4 Docker services (postgres, web, api, indexer).
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-web-ui/07-CONTEXT.md
@.planning/phases/07-web-ui/07-RESEARCH.md
@.planning/phases/07-web-ui/07-06-SUMMARY.md

<interfaces>
From docker-compose.prod.yml (existing):
```yaml
services:
  postgres:   # port 5433:5432, healthcheck pg_isready
  migrate:    # alembic upgrade head, depends on postgres
  web:        # port 3003:3000, depends on postgres, migrate
  indexer:    # depends on postgres, migrate
```

From .github/workflows/deploy.yml (existing):
- SSH deploy to server
- docker compose build + restart
- Rolling restart: web then indexer
- Health check after deploy

From scripts/deploy.sh (existing):
- SSH-based deployment
- Rolling restart order
- Health check verification
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FastAPI Dockerfile + docker-compose update + deploy workflow</name>
  <files>api/Dockerfile, docker-compose.prod.yml, .github/workflows/deploy.yml, scripts/deploy.sh</files>
  <action>
1. **Create `api/Dockerfile`:**
   ```dockerfile
   FROM python:3.11-slim
   WORKDIR /app
   COPY requirements.txt .
   RUN pip install --no-cache-dir -r requirements.txt
   COPY . .
   EXPOSE 8000
   CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "2"]
   ```
   Build context is project root (same pattern as indexers/Dockerfile) so `db/`, `config.py`, `indexers/`, `engine/`, `reports/`, `verify/` are all available.

2. **Update `docker-compose.prod.yml`:** Add `api` service:
   - build: context=., dockerfile=api/Dockerfile
   - ports: "8000:8000"
   - environment: DATABASE_URL, RP_ID, RP_NAME, ORIGIN, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_SES_REGION, ALLOWED_ORIGINS, SESSION_SECURE=true, SECRET_KEY
   - depends_on: postgres (healthy), migrate (completed_successfully)
   - command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --workers 2
   - restart: unless-stopped
   - networks: axiom-net
   - healthcheck: test CMD curl -f http://localhost:8000/health || exit 1, interval 10s, timeout 5s, retries 5

   Update `web` service: add `NEXT_PUBLIC_API_URL=http://api:8000` to environment. Remove DATABASE_URL from web (Next.js no longer talks to DB directly).

   Add `api` to migrate depends (api needs schema ready).

3. **Update `.github/workflows/deploy.yml`:** Add api to the build and deploy steps. Rolling restart order: web, api, indexer (user-facing services first). Add health check for api service (curl /health).

4. **Update `scripts/deploy.sh`:** Add api container to rolling restart sequence. Add health check step for api.

5. **Delete `web/app/api/` directory:** Remove all Next.js API routes. This is safe because Plan 06 has already rewired all frontend fetches to FastAPI. Delete the entire directory tree:
   - web/app/api/acb/
   - web/app/api/accountant/
   - web/app/api/admin/
   - web/app/api/assets/
   - web/app/api/auth/
   - web/app/api/defi/
   - web/app/api/exchange-rates/
   - web/app/api/exchanges/
   - web/app/api/health/
   - web/app/api/import/
   - web/app/api/indexers/
   - web/app/api/phantom-auth/
   - web/app/api/portfolio/
   - web/app/api/price/
   - web/app/api/price-warnings/
   - web/app/api/prices/
   - web/app/api/reports/
   - web/app/api/spam/
   - web/app/api/staking/
   - web/app/api/sync/
   - web/app/api/tally/
   - web/app/api/transactions/
   - web/app/api/upload-file/
   - web/app/api/user/
   - web/app/api/validators/
   - web/app/api/wallets/
   Also remove `web/lib/db.ts` (no longer needed — FastAPI handles all DB access).
   Also remove `web/lib/auth-db.ts` if it exists (auth moved to FastAPI).
   Keep `web/lib/auth.ts` only if it contains client-side-only logic; otherwise remove.
   Keep `web/middleware.ts` only for non-API middleware (e.g., redirects); remove rate limiting and auth checks (FastAPI handles those).
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && docker compose -f docker-compose.prod.yml config --quiet 2>&1 && echo "Docker Compose valid" || echo "Docker Compose invalid"</automated>
  </verify>
  <done>api/Dockerfile exists. docker-compose.prod.yml has 4 services (postgres, web, api, indexer). Deploy workflow includes api. Next.js API routes deleted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 7 Web UI: FastAPI backend with auth (passkey + OAuth + magic link), wallet management with auto-chain pipeline, transaction ledger with classification editing, report generation with previews and downloads, verification dashboard, all wired to Next.js frontend, Docker deployment ready.</what-built>
  <how-to-verify>
    1. Start the stack: `docker compose -f docker-compose.prod.yml up --build -d`
    2. Wait for health checks: `docker compose -f docker-compose.prod.yml ps` (all services should be "healthy")
    3. Visit http://localhost:3003 — should see login page
    4. Register with a passkey (or use email magic link if configured)
    5. After login, verify dashboard loads (may be empty for new user)
    6. Add a NEAR wallet — verify pipeline progress bar appears and progresses through stages
    7. Navigate to Transactions page — verify ledger loads with filters
    8. Navigate to Reports page — verify preview tabs show data (if pipeline complete)
    9. Navigate to Verification page — verify issue summary displays
    10. Check API health: `curl http://localhost:8000/health` returns {"status":"ok"}
    11. Check no old API routes: `ls web/app/api/` should not exist
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- docker-compose.prod.yml validates without errors
- api/Dockerfile builds successfully
- web/app/api/ directory deleted
- All 4 services start and pass health checks
- End-to-end flow works from login to report generation
</verification>

<success_criteria>
- Production Docker stack has 4 services: postgres, web (Next.js), api (FastAPI), indexer
- FastAPI health check passes
- Next.js API routes fully removed
- Deploy workflow updated for new api service
- User confirms end-to-end flow works
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-ui/07-07-SUMMARY.md`
</output>
