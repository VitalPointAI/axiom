---
phase: 07-web-ui
plan: 05
type: execute
wave: 3
depends_on: ["07-01"]
files_modified:
  - api/routers/reports.py
  - api/routers/verification.py
  - api/routers/exchanges.py
  - api/schemas/reports.py
  - api/schemas/verification.py
  - tests/test_api_reports.py
  - tests/test_api_verification.py
autonomous: true
requirements:
  - UI-06
  - UI-07

must_haves:
  truths:
    - "User can generate a tax report package via job queue"
    - "User can download generated report files (CSV + PDF)"
    - "User can see inline data previews for report tabs"
    - "User can view verification issues grouped by diagnosis category"
    - "User can take resolution actions on verification issues"
    - "Admin-only specialist override for needs_review items"
  artifacts:
    - path: "api/routers/reports.py"
      provides: "Report generation, preview, and download endpoints"
      min_lines: 100
    - path: "api/routers/verification.py"
      provides: "Verification dashboard endpoints"
      min_lines: 80
  key_links:
    - from: "api/routers/reports.py"
      to: "indexing_jobs table"
      via: "INSERT generate_reports job"
      pattern: "generate_reports"
    - from: "api/routers/reports.py"
      to: "output/{year}_tax_package/"
      via: "FileResponse for downloads"
      pattern: "FileResponse"
    - from: "api/routers/verification.py"
      to: "verification_results table"
      via: "SELECT grouped by diagnosis_category"
      pattern: "verification_results"
---

<objective>
Build the report generation/download API and verification dashboard API. Reports use job queue + polling pattern with inline previews. Verification shows issues grouped by type with actionable resolution guidance.

Purpose: Reports are the final deliverable (tax package for accountant). Verification dashboard surfaces data quality issues that must be resolved before reports are accurate.
Output: Report generation/preview/download endpoints + verification issue dashboard with resolution actions.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-web-ui/07-CONTEXT.md
@.planning/phases/07-web-ui/07-RESEARCH.md
@.planning/phases/07-web-ui/07-01-SUMMARY.md

<interfaces>
From api/dependencies.py (Plan 01):
```python
async def get_effective_user(...) -> dict
async def require_admin(user) -> dict  # raises 403 if not admin
```

From existing pipeline (reports/generate.py - Phase 6):
```python
class PackageBuilder:
    def build_package(self, user_id, year, tax_treatment, specialist_override=False):
        # Generates all CSV + PDF reports to output/{year}_tax_package/
        # Returns list of file paths
```

From existing pipeline (indexers/service.py - Phase 6):
- Job type "generate_reports" handled by ReportHandler
- cursor JSON: {"year": 2025, "specialist_override": false}

From db schema (verification_results - migration 005):
```sql
CREATE TABLE verification_results (
    id SERIAL PRIMARY KEY,
    wallet_id INTEGER REFERENCES wallets(id),
    token_symbol TEXT NOT NULL,
    verification_type TEXT NOT NULL,
    status TEXT NOT NULL,
    expected_balance NUMERIC(40,0),
    actual_balance NUMERIC(40,0),
    discrepancy NUMERIC(40,0),
    diagnosis_category TEXT,
    diagnosis_detail JSONB,
    needs_review BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(wallet_id, token_symbol)
);
```

Report modules from Phase 6:
- reports/capital_gains.py: CapitalGainsReport
- reports/income.py: IncomeReport
- reports/ledger.py: LedgerReport
- reports/t1135.py: T1135Checker
- reports/superficial.py: SuperficialLossReport
- reports/export.py: KoinlyExport, AccountingExporter
- reports/inventory.py: InventoryHoldingsReport, COGSReport
- reports/business.py: BusinessIncomeStatement
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Report generation via job queue + inline previews + file downloads</name>
  <files>api/routers/reports.py, api/schemas/reports.py, api/routers/exchanges.py, tests/test_api_reports.py</files>
  <behavior>
    - test_generate_report: POST /api/reports/generate creates generate_reports job, returns job_id
    - test_generate_requires_auth: unauthenticated returns 401
    - test_specialist_override_admin_only: specialist_override=true requires is_admin
    - test_report_preview_capital_gains: GET /api/reports/preview/capital-gains returns first 50 rows from DB
    - test_report_preview_income: GET /api/reports/preview/income returns monthly summary
    - test_download_report: GET /api/reports/download/{year}/{filename} returns FileResponse
    - test_download_not_found: missing file returns 404
    - test_exchange_import: POST /api/exchanges/import accepts CSV upload, queues file_import job
  </behavior>
  <action>
1. **`api/schemas/reports.py`:**
   - ReportGenerateRequest: year (int), tax_treatment (str, default="capital"), specialist_override (bool, default=False)
   - ReportGenerateResponse: job_id (int), status (str)
   - ReportPreviewResponse: rows (list[dict]), total (int), report_type (str)
   - ReportFileResponse: files (list with name, size, url), year (int), generated_at (str)

2. **`api/routers/reports.py`:**
   - POST /api/reports/generate: Check if specialist_override=true requires admin (require_admin dependency). INSERT indexing_job with job_type="generate_reports", cursor=json.dumps({"year": year, "tax_treatment": treatment, "specialist_override": override}), user_id=effective_user_id. Return job_id.
   - GET /api/reports/preview/{report_type}: Inline data preview endpoints. For each report type, run a lightweight query (LIMIT 50) against the same tables the report modules use:
     - capital-gains: SELECT from capital_gains_ledger WHERE user_id=... ORDER BY disposal_date LIMIT 50
     - income: SELECT token_symbol, DATE_TRUNC('month', income_date) as month, SUM(amount), SUM(fmv_cad) FROM income_ledger GROUP BY 1,2
     - ledger: reuse LedgerReport UNION ALL pattern LIMIT 50
     - t1135: SELECT from acb_snapshots for peak cost check
     - superficial-losses: SELECT from capital_gains_ledger WHERE is_superficial_loss=true
     - holdings: latest acb_snapshots per token
   - GET /api/reports/download/{year}: List all files in `output/{year}_tax_package/`. Return file list with download URLs.
   - GET /api/reports/download/{year}/{filename}: Serve file via FastAPI FileResponse. Validate filename (no path traversal). Check file exists in `output/{year}_tax_package/`.
   - GET /api/reports/status: Check if reports exist for given year (cached check).

3. **`api/routers/exchanges.py`:** (small, include here)
   - POST /api/exchanges/import: Accept file upload (multipart). Save to temp, INSERT file_imports row with SHA-256 hash, queue file_import job. Same pattern as existing web/app/api/upload-file/route.ts.
   - GET /api/exchanges: List supported exchanges from supported_exchanges table.

4. **Tests:** Mock DB for preview queries. Mock file system for download. Test job creation, admin-only override, file serving.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_api_reports.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Report generation creates job. Preview endpoints return limited data from DB. Download serves files. Exchange import queues file processing job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verification dashboard with issue grouping and resolution actions</name>
  <files>api/routers/verification.py, api/schemas/verification.py, tests/test_api_verification.py</files>
  <behavior>
    - test_verification_summary: GET /api/verification/summary returns issue counts grouped by diagnosis_category
    - test_verification_issues: GET /api/verification/issues?category=missing_staking_rewards returns detailed issues
    - test_verification_resolve: POST /api/verification/resolve/{id} marks issue as resolved
    - test_verification_resync: POST /api/verification/resync/{id} queues re-sync job for affected wallet
    - test_needs_review_count: GET /api/verification/needs-review-count returns total unresolved count
    - test_user_isolation: only shows verification results for user's wallets
  </behavior>
  <action>
1. **`api/schemas/verification.py`:**
   - IssueGroup: category (str), count (int), severity (str: "high"|"medium"|"low"), description (str), suggested_action (str)
   - VerificationSummary: groups (list[IssueGroup]), total_issues (int), needs_review_count (int)
   - VerificationIssue: id, wallet_id, account_id, token_symbol, verification_type, status, expected_balance (str), actual_balance (str), discrepancy (str), diagnosis_category, diagnosis_detail, needs_review, created_at
   - ResolveRequest: resolution_notes (str), mark_reviewed (bool, default=True)

2. **`api/routers/verification.py`:**
   - GET /api/verification/summary: Query verification_results for user's wallets. GROUP BY diagnosis_category with COUNT. Map categories to severity and suggested actions:
     - missing_staking_rewards -> high, "Re-sync staking rewards"
     - uncounted_fees -> medium, "Review fee calculations"
     - unindexed_period -> high, "Re-index wallet for missing period"
     - classification_error -> medium, "Review transaction classifications"
     - duplicates -> low, "Review potential duplicate transactions"
   Also include counts from transaction_classifications WHERE needs_review=true and capital_gains_ledger WHERE needs_review=true.

   - GET /api/verification/issues: Accept ?category filter. Return detailed issues for that category with wallet account_id included (JOIN wallets).
   - POST /api/verification/resolve/{id}: Verify issue belongs to user's wallet. UPDATE verification_results SET needs_review=false, status='resolved'. Accept resolution_notes.
   - POST /api/verification/resync/{id}: Get wallet_id from verification result. Queue appropriate re-sync jobs (e.g., staking_sync for missing_staking_rewards, full_sync for unindexed_period).
   - GET /api/verification/needs-review-count: Quick count of all needs_review=true items across verification_results + transaction_classifications + capital_gains_ledger for user.

3. **Tests:** Mock DB with sample verification_results. Test grouping, filtering, resolution, re-sync job creation.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_api_verification.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Verification summary groups issues by category with counts and suggested actions. Resolution marks issues reviewed. Re-sync queues appropriate jobs.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_api_reports.py tests/test_api_verification.py -x -q` all pass
- POST /api/reports/generate creates correct job type
- GET /api/reports/preview/* returns limited data
- GET /api/verification/summary groups by diagnosis_category
- POST /api/verification/resync creates correct job
</verification>

<success_criteria>
- Report generation via job queue with polling pattern
- Inline previews show first N rows for each report type
- File download serves generated CSV/PDF files
- Verification dashboard groups issues by type with severity
- Resolution actions (mark reviewed, re-sync) work
- Admin-only specialist override for report generation
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-ui/07-05-SUMMARY.md`
</output>
