---
phase: 07-web-ui
plan: 05
wave: 3
depends_on: [07-02]
autonomous: true
requirements: [UI-06, UI-07]
files_modified:
  - web/app/reports/page.tsx
  - web/components/report-generator.tsx
  - web/components/verification-status.tsx
  - web/app/api/reports/generate/route.ts
  - web/app/api/reports/download/[id]/route.ts
  - web/app/api/verification/route.ts
must_haves:
  truths:
    - Users can select tax year and generate reports
    - Reports can be downloaded as CSV/PDF
    - Verification status shows reconciliation results
    - Issues are displayed with actionable guidance
  artifacts:
    - path: web/app/reports/page.tsx
      provides: Report generation and verification view
    - path: web/app/api/reports/generate/route.ts
      provides: Report generation API
---

<objective>
Build the reports page with tax report generation, download functionality, and verification status dashboard showing data quality.
</objective>

<context>
Reports system (Phase 6) generates:
- Capital gains/losses report
- Income summary (staking rewards)
- Full transaction ledger
- T1135 status check

Verification (Phase 5) checks:
- Balance reconciliation
- Duplicate detection
- Gap detection

Reports should be generated on-demand and cached for download.
</context>

<tasks>
  <task type="auto">
    <name>Create reports page layout</name>
    <files>web/app/reports/page.tsx</files>
    <action>
    Build reports page with two sections:
    1. Report Generator - select year, generate reports
    2. Data Verification - show reconciliation status
    
    Card-based layout with clear sections.
    </action>
    <verify>Reports page loads with both sections</verify>
    <done>Reports page layout complete</done>
  </task>

  <task type="auto">
    <name>Build report generator</name>
    <files>web/components/report-generator.tsx, web/app/api/reports/generate/route.ts</files>
    <action>
    Create ReportGenerator component:
    - Tax year selector (2024, 2025)
    - Report type checkboxes:
      - [ ] Capital Gains Summary
      - [ ] Income Summary
      - [ ] Full Transaction Ledger
      - [ ] T1135 Check
    - Generate button (shows progress)
    - Download buttons when ready
    
    API: POST /api/reports/generate
    ```typescript
    { year: number, types: string[] }
    Response: { reportId: string, status: 'generating' | 'ready' }
    ```
    
    Poll for completion, then show download links.
    </action>
    <verify>Can generate and download reports</verify>
    <done>Report generation works</done>
  </task>

  <task type="auto">
    <name>Implement report download</name>
    <files>web/app/api/reports/download/[id]/route.ts</files>
    <action>
    Serve generated reports:
    GET /api/reports/download/[id]?format=csv
    GET /api/reports/download/[id]?format=pdf
    
    Set proper Content-Disposition headers.
    Reports cached for 24 hours.
    Include Koinly-compatible format option.
    </action>
    <verify>Downloads work in browser</verify>
    <done>Report downloads working</done>
  </task>

  <task type="auto">
    <name>Build verification dashboard</name>
    <files>web/components/verification-status.tsx, web/app/api/verification/route.ts</files>
    <action>
    Create VerificationStatus showing:
    - Overall status badge (All Clear / Issues Found)
    - Per-wallet reconciliation:
      - Wallet address
      - Calculated balance
      - On-chain balance
      - Difference (highlighted if >0.01)
    - Issue list:
      - Type (duplicate, gap, discrepancy)
      - Severity (warning, error)
      - Description
      - Suggested action
    
    "Run Verification" button to refresh checks.
    </action>
    <verify>Verification shows real reconciliation data</verify>
    <done>Verification dashboard complete</done>
  </task>
</tasks>

<verification>
1. /reports page shows generator and verification sections
2. Can select tax year
3. Can select report types
4. Generate button creates reports
5. Download buttons provide CSV files
6. Koinly format option available
7. Verification shows per-wallet status
8. Discrepancies highlighted clearly
9. Run verification triggers fresh check
10. Issues show actionable guidance
</verification>
