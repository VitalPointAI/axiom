---
phase: 12-user-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/versions/010_onboarding_columns.py
  - api/routers/preferences.py
  - api/routers/__init__.py
  - api/main.py
  - api/routers/wallets.py
  - tests/test_api_preferences.py
autonomous: true
requirements: [ONBOARD-01, ONBOARD-05, ONBOARD-06]

must_haves:
  truths:
    - "GET /api/preferences returns onboarding_completed_at and dismissed_banners for authenticated user"
    - "POST /api/preferences/complete-onboarding sets onboarding_completed_at to NOW() idempotently"
    - "PATCH /api/preferences/dismiss-banner merges banner key into dismissed_banners JSONB"
    - "GET /api/wallets/suggestions returns discovered linked wallets from WalletGraph"
    - "Existing users with wallets but NULL onboarding_completed_at are not broken"
  artifacts:
    - path: "db/migrations/versions/010_onboarding_columns.py"
      provides: "onboarding_completed_at TIMESTAMPTZ + dismissed_banners JSONB on users table"
      contains: "onboarding_completed_at"
    - path: "api/routers/preferences.py"
      provides: "Preferences CRUD endpoints"
      exports: ["router"]
    - path: "tests/test_api_preferences.py"
      provides: "Tests for all 3 preferences endpoints + JSONB merge + idempotent completion"
      min_lines: 60
  key_links:
    - from: "api/routers/preferences.py"
      to: "users table"
      via: "SELECT/UPDATE onboarding_completed_at, dismissed_banners"
      pattern: "onboarding_completed_at|dismissed_banners"
    - from: "api/routers/wallets.py"
      to: "engine/wallet_graph.py"
      via: "WalletGraph.suggest_wallet_discovery()"
      pattern: "suggest_wallet_discovery"
    - from: "api/main.py"
      to: "api/routers/preferences.py"
      via: "include_router(preferences_router)"
      pattern: "preferences_router"
---

<objective>
Create the backend foundation for user onboarding: Alembic migration 010 adding `onboarding_completed_at` and `dismissed_banners` columns to users table, a preferences API router (GET/POST/PATCH), and a wallet suggestions endpoint wrapping WalletGraph discovery.

Purpose: All frontend onboarding features (wizard, banners, inline guidance) depend on these backend endpoints for state tracking and data retrieval.
Output: Migration 010, `api/routers/preferences.py`, wallet suggestions endpoint, and tests.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-user-onboarding/12-CONTEXT.md
@.planning/phases/12-user-onboarding/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From api/dependencies.py:
```python
def get_effective_user(user=Depends(get_current_user), ...) -> dict:
    # Returns: { user_id, near_account_id, is_admin, email, username, codename, viewing_as_user_id, permission_level }

def get_pool_dep():
    # Returns psycopg2 SimpleConnectionPool
```

From api/routers/__init__.py:
```python
from api.routers.wallets import router as wallets_router  # pattern for new router imports
```

From api/main.py:
```python
application.include_router(wallets_router)  # pattern for mounting new routers
```

From db/migrations/versions/009_auth_user_mapping.py:
```python
revision = "009"
down_revision = "008"
# Migration 010 must use: revision = "010", down_revision = "009"
```

From engine/wallet_graph.py:
```python
class WalletGraph:
    def suggest_wallet_discovery(self, user_id, min_transfers=3):
        # Returns list of { address, chain, transfer_count, related_to }
```

From api/routers/wallets.py:
```python
router = APIRouter(prefix="/api/wallets", tags=["wallets"])
# Wallet suggestions endpoint will be added here at GET /api/wallets/suggestions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 010 + preferences router + wallet suggestions + tests</name>
  <files>
    db/migrations/versions/010_onboarding_columns.py,
    api/routers/preferences.py,
    api/routers/__init__.py,
    api/main.py,
    api/routers/wallets.py,
    tests/test_api_preferences.py
  </files>
  <action>
1. Create `db/migrations/versions/010_onboarding_columns.py`:
   - revision = "010", down_revision = "009"
   - upgrade(): `ALTER TABLE users ADD COLUMN IF NOT EXISTS onboarding_completed_at TIMESTAMPTZ, ADD COLUMN IF NOT EXISTS dismissed_banners JSONB DEFAULT '{}'`
   - downgrade(): `ALTER TABLE users DROP COLUMN IF EXISTS onboarding_completed_at, DROP COLUMN IF EXISTS dismissed_banners`
   - Use `op.execute()` with raw SQL (same pattern as migration 006 and 009)

2. Create `api/routers/preferences.py` with 3 endpoints:
   - `GET /api/preferences` — query users table for `onboarding_completed_at, dismissed_banners` WHERE id = user_id. Return `{ onboarding_completed_at: str|null, dismissed_banners: dict }`. Use `get_effective_user` + `get_pool_dep` via Depends. Wrap psycopg2 calls in `run_in_threadpool`.
   - `POST /api/preferences/complete-onboarding` — `UPDATE users SET onboarding_completed_at = COALESCE(onboarding_completed_at, NOW()) WHERE id = %s`. Idempotent (COALESCE prevents overwriting). Return `{ onboarding_completed_at: str }`.
   - `PATCH /api/preferences/dismiss-banner` — Accept `{ banner_key: str }` in request body (Pydantic model `DismissBannerRequest`). `UPDATE users SET dismissed_banners = COALESCE(dismissed_banners, '{}'::jsonb) || %s::jsonb WHERE id = %s` with `json.dumps({banner_key: True})`. Return `{ dismissed_banners: dict }`.
   - All endpoints use `run_in_threadpool` for psycopg2 calls, `get_effective_user` for user context (accountant delegation support), and `pool.getconn()`/`pool.putconn()` in try/finally.
   - Router prefix: `/api/preferences`, tags: `["preferences"]`

3. Register the preferences router:
   - In `api/routers/__init__.py`: add `from api.routers.preferences import router as preferences_router`
   - In `api/main.py`: add `from api.routers import preferences_router` to imports and `application.include_router(preferences_router)` in create_app()

4. Add wallet suggestions endpoint to `api/routers/wallets.py`:
   - `GET /api/wallets/suggestions` — Import WalletGraph, instantiate with pool, call `suggest_wallet_discovery(user_id, min_transfers=3)`. Return `{ suggestions: list }` where each item has `{ address, chain, transfer_count, related_to }`.
   - IMPORTANT: Register this route BEFORE any `/{wallet_id}` path param routes (FastAPI matches in registration order — "suggestions" would be parsed as an integer wallet_id otherwise). Same pattern as `/api/jobs/active` registered before `/{job_id}/status`.
   - Wrap in `run_in_threadpool`. Handle case where WalletGraph has no data gracefully (return empty list).

5. Create `tests/test_api_preferences.py`:
   - Follow existing test patterns (see tests/test_api_wallets.py): mock pool via `dependency_overrides[get_pool_dep]`, mock `get_effective_user` override, use `TestClient(app)`.
   - Test GET /api/preferences returns both columns with correct types
   - Test GET /api/preferences handles NULL dismissed_banners (returns empty dict)
   - Test POST /api/preferences/complete-onboarding sets timestamp
   - Test POST /api/preferences/complete-onboarding is idempotent (second call doesn't overwrite)
   - Test PATCH /api/preferences/dismiss-banner adds key to JSONB
   - Test PATCH /api/preferences/dismiss-banner with missing banner_key returns 422
   - At least 6 tests total

IMPORTANT: Run `ruff check` before finishing — pre-commit hook requires lint clean.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_api_preferences.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Migration 010 exists with IF NOT EXISTS idempotency
    - GET /api/preferences returns onboarding_completed_at + dismissed_banners
    - POST complete-onboarding is idempotent (COALESCE pattern)
    - PATCH dismiss-banner merges JSONB atomically
    - GET /api/wallets/suggestions returns WalletGraph results
    - All 6+ tests pass
    - ruff lint clean
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_api_preferences.py -x -q` — all tests pass
- `ruff check api/routers/preferences.py db/migrations/versions/010_onboarding_columns.py` — no lint errors
- `python -c "from api.routers.preferences import router; print(router.prefix)"` — prints `/api/preferences`
</verification>

<success_criteria>
- Migration 010 adds onboarding_completed_at and dismissed_banners to users table
- Preferences API (GET/POST/PATCH) operational with correct auth
- Wallet suggestions endpoint returns WalletGraph discovery results
- All tests pass, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-user-onboarding/12-01-SUMMARY.md`
</output>
