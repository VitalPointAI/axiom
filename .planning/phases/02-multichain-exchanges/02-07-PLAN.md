---
phase: 02-multichain-exchanges
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/versions/002b_add_updated_at.py
  - indexers/dedup_handler.py
  - web/app/api/upload-file/route.ts
autonomous: true
requirements: [DATA-04, DATA-05]
gap_closure: true

must_haves:
  truths:
    - "file_import jobs complete without column-not-found error on updated_at"
    - "dedup_scan jobs match on-chain transactions by epoch-integer block_timestamp comparison"
    - "POST /api/upload-file wallet upsert succeeds without ON CONFLICT constraint error"
  artifacts:
    - path: "db/migrations/versions/002b_add_updated_at.py"
      provides: "updated_at column for file_imports and exchange_transactions"
      contains: "add_column.*updated_at"
    - path: "indexers/dedup_handler.py"
      provides: "Epoch-integer conversion for block_timestamp window query"
      contains: "int.*timestamp"
    - path: "web/app/api/upload-file/route.ts"
      provides: "Correct ON CONFLICT clause matching wallets unique constraint"
      contains: "ON CONFLICT (user_id, account_id, chain)"
  key_links:
    - from: "indexers/file_handler.py"
      to: "file_imports.updated_at"
      via: "SET updated_at = NOW()"
      pattern: "updated_at"
    - from: "indexers/dedup_handler.py"
      to: "transactions.block_timestamp"
      via: "BETWEEN with epoch integers"
      pattern: "BETWEEN.*window_start.*window_end"
    - from: "web/app/api/upload-file/route.ts"
      to: "wallets unique constraint"
      via: "ON CONFLICT (user_id, account_id, chain)"
      pattern: "ON CONFLICT"
---

<objective>
Fix three blocker runtime bugs identified by Phase 2 verification: missing updated_at columns in migration 002, datetime-vs-BIGINT type mismatch in DedupHandler, and wrong ON CONFLICT column in the upload route.

Purpose: Without these fixes, file imports, dedup scans, and file uploads will all crash at runtime despite passing unit tests (which mock the database).
Output: One new Alembic migration (002b) and two 1-line code fixes.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multichain-exchanges/02-VERIFICATION.md

<interfaces>
<!-- From db/migrations/versions/002_multichain_exchanges.py: -->
<!-- exchange_transactions table has created_at but NO updated_at -->
<!-- file_imports table has created_at but NO updated_at -->

<!-- From indexers/file_handler.py lines 94, 197, 253: -->
<!-- SET updated_at = NOW() on file_imports -->

<!-- From indexers/dedup_handler.py line 192: -->
<!-- SET updated_at = NOW() on exchange_transactions -->

<!-- From indexers/dedup_handler.py lines 154-168: -->
<!-- window_start = tx_date - timedelta(minutes=10) [datetime object] -->
<!-- window_end = tx_date + timedelta(minutes=10) [datetime object] -->
<!-- block_timestamp BETWEEN %s AND %s [column is BIGINT epoch seconds] -->

<!-- From db/migrations/versions/001_initial_schema.py: -->
<!-- transactions.block_timestamp is sa.BigInteger (BIGINT, Unix epoch seconds) -->

<!-- From web/app/api/upload-file/route.ts line 118: -->
<!-- ON CONFLICT (account_id) DO NOTHING — wrong, wallets UNIQUE is (user_id, account_id, chain) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add updated_at columns via Alembic migration 002b</name>
  <files>db/migrations/versions/002b_add_updated_at.py</files>
  <action>Create a new Alembic migration file `db/migrations/versions/002b_add_updated_at.py` with revision='002b', down_revision='002'. In upgrade(): use op.add_column() to add `updated_at TIMESTAMPTZ DEFAULT NOW()` to both `file_imports` and `exchange_transactions` tables. In downgrade(): use op.drop_column() to remove the column from both tables. Follow the exact pattern from migration 001 which uses `sa.Column("updated_at", sa.DateTime(timezone=True), server_default=sa.text("now()"), nullable=True)`. Mark nullable=True since existing rows will get NULL (the DEFAULT only applies to new inserts; existing rows populated by a subsequent `UPDATE ... SET updated_at = NOW()` is optional).</action>
  <verify>
    <automated>python -c "import importlib.util; spec = importlib.util.spec_from_file_location('m', 'db/migrations/versions/002b_add_updated_at.py'); mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); assert mod.revision == '002b'; assert mod.down_revision == '002'; print('Migration 002b valid')"</automated>
  </verify>
  <done>Migration 002b exists with correct revision chain (002 -> 002b), adds updated_at to both file_imports and exchange_transactions</done>
</task>

<task type="auto">
  <name>Task 2: Convert dedup window_start/window_end to epoch integers</name>
  <files>indexers/dedup_handler.py</files>
  <action>In indexers/dedup_handler.py, find lines 154-155 where `window_start` and `window_end` are computed as datetime objects. After those lines (before the cur.execute on line 157), convert both to Unix epoch integers:

```python
window_start_epoch = int(window_start.timestamp())
window_end_epoch = int(window_end.timestamp())
```

Then on line 168, change the query parameters from `(user_id, asset, on_chain_direction, window_start, window_end)` to `(user_id, asset, on_chain_direction, window_start_epoch, window_end_epoch)`.

This ensures the BETWEEN comparison operates on BIGINT values matching the block_timestamp column type. Do NOT modify the SQL query itself or any other logic.</action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python -m pytest tests/test_dedup_handler.py -x -q</automated>
  </verify>
  <done>DedupHandler passes epoch integers to block_timestamp BETWEEN query; all existing dedup tests still pass</done>
</task>

<task type="auto">
  <name>Task 3: Fix ON CONFLICT clause in upload-file route</name>
  <files>web/app/api/upload-file/route.ts</files>
  <action>In web/app/api/upload-file/route.ts, find line 118 which reads:

```
ON CONFLICT (account_id) DO NOTHING
```

Change it to:

```
ON CONFLICT (user_id, account_id, chain) DO NOTHING
```

This matches the wallets table unique constraint `UNIQUE(user_id, account_id, chain)` from migration 001. No other changes needed.</action>
  <verify>
    <automated>grep -n "ON CONFLICT (user_id, account_id, chain)" web/app/api/upload-file/route.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Upload route uses correct ON CONFLICT (user_id, account_id, chain) matching the wallets table constraint</done>
</task>

</tasks>

<verification>
All three gaps from 02-VERIFICATION.md are addressed:
1. Gap 1 (updated_at columns): Migration 002b adds the column to both tables
2. Gap 2 (block_timestamp type mismatch): Epoch integer conversion before query
3. Gap 3 (wrong ON CONFLICT): Corrected to match (user_id, account_id, chain) constraint

Existing tests must still pass:
- `python -m pytest tests/test_dedup_handler.py -x -q`
- `python -m pytest tests/test_exchange_parsers.py -x -q`
- `python -m pytest tests/test_evm_fetcher.py -x -q`
</verification>

<success_criteria>
- Migration 002b exists with correct revision chain and adds updated_at to both tables
- DedupHandler block_timestamp query uses epoch integers, not datetime objects
- Upload route ON CONFLICT matches wallets (user_id, account_id, chain) constraint
- All existing Phase 2 tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-multichain-exchanges/02-07-SUMMARY.md`
</output>
