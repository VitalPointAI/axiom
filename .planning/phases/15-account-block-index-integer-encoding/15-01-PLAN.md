---
phase: 15-account-block-index-integer-encoding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/versions/020_integer_encoded_index.py
autonomous: true
requirements: [INT-01]

must_haves:
  truths:
    - "account_dictionary table exists with SERIAL id and TEXT account_id UNIQUE"
    - "account_block_index_v2 table exists with INTEGER account_int and INTEGER segment_start"
    - "Composite primary key on (account_int, segment_start) prevents duplicates"
    - "Original account_block_index table is untouched and still queryable"
    - "account_indexer_state table is untouched (shared by both pipelines)"
  artifacts:
    - path: "db/migrations/versions/020_integer_encoded_index.py"
      provides: "Alembic migration creating dictionary + v2 tables"
      contains: "account_dictionary"
      min_lines: 30
  key_links:
    - from: "db/migrations/versions/020_integer_encoded_index.py"
      to: "PostgreSQL schema"
      via: "alembic upgrade head"
      pattern: "op\\.create_table"
---

<objective>
Create Alembic migration 020 that adds the `account_dictionary` table (maps account strings to compact integer IDs) and the `account_block_index_v2` table (stores integer-encoded account_int + segment_start pairs). The original `account_block_index` table is NOT dropped -- both coexist during transition.

Purpose: This migration is the foundation for the entire phase. All subsequent plans depend on these tables existing. The v2 table uses INTEGER (4 bytes) for both columns, combined with segment-based indexing (1K-block granules) to achieve massive storage reduction.

Output: One Alembic migration file (020) that can be applied via `alembic upgrade head`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-account-block-index-integer-encoding/15-CONTEXT.md
@.planning/phases/15-account-block-index-integer-encoding/15-RESEARCH.md

<interfaces>
<!-- Current migration 018 pattern (the table we're complementing, not replacing): -->

From db/migrations/versions/018_account_block_index.py:
```python
revision = "018"
down_revision = "017"

def upgrade():
    op.create_table(
        "account_block_index",
        sa.Column("account_id", sa.Text(), nullable=False),
        sa.Column("block_height", sa.BigInteger(), nullable=False),
        sa.PrimaryKeyConstraint("account_id", "block_height"),
    )
```

Current migration head: 019 (019_waitlist_signups.py)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration 020 — account_dictionary + account_block_index_v2</name>
  <files>db/migrations/versions/020_integer_encoded_index.py</files>
  <action>
Create migration 020 with `revision = "020"` and `down_revision = "019"`.

The upgrade() function creates two tables:

1. **account_dictionary** table:
   - `id` — `sa.Integer(), primary_key=True, autoincrement=True` (SERIAL)
   - `account_id` — `sa.Text(), nullable=False, unique=True`
   - Create explicit index: `ix_account_dictionary_account_id` on `["account_id"]` with `unique=True`

2. **account_block_index_v2** table:
   - `account_int` — `sa.Integer(), nullable=False`
   - `segment_start` — `sa.Integer(), nullable=False`
   - `sa.PrimaryKeyConstraint("account_int", "segment_start")`
   - Create lookup index: `ix_abiv2_account_segment` on `["account_int", "segment_start"]`

The downgrade() function drops in reverse order:
1. Drop `ix_abiv2_account_segment` index
2. Drop `account_block_index_v2` table
3. Drop `ix_account_dictionary_account_id` index
4. Drop `account_dictionary` table

IMPORTANT: Do NOT touch the original `account_block_index` table or `account_indexer_state` table in any way. Both old and new tables coexist during transition.

Column naming: Use `segment_start` (not `block_height`) since this column stores the segment start value (block_height // 1000 * 1000), not an exact block height. This makes the semantics clear to anyone reading the schema.

Follow the existing migration docstring pattern from 018 — include a module-level docstring explaining what this migration does and why.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/Axiom && python3 -c "
import importlib.util, sys
spec = importlib.util.spec_from_file_location('m020', 'db/migrations/versions/020_integer_encoded_index.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
assert mod.revision == '020', f'Expected revision 020, got {mod.revision}'
assert mod.down_revision == '019', f'Expected down_revision 019, got {mod.down_revision}'
assert hasattr(mod, 'upgrade'), 'Missing upgrade()'
assert hasattr(mod, 'downgrade'), 'Missing downgrade()'
# Check source for required table names
import inspect
src = inspect.getsource(mod.upgrade)
assert 'account_dictionary' in src, 'Missing account_dictionary table creation'
assert 'account_block_index_v2' in src, 'Missing account_block_index_v2 table creation'
assert 'segment_start' in src, 'Missing segment_start column'
assert 'account_int' in src, 'Missing account_int column'
print('Migration 020 structure validated OK')
"
    </automated>
  </verify>
  <done>
Migration 020 file exists at `db/migrations/versions/020_integer_encoded_index.py` with:
- account_dictionary table (id SERIAL PK, account_id TEXT UNIQUE NOT NULL)
- account_block_index_v2 table (account_int INTEGER, segment_start INTEGER, composite PK)
- Proper indexes on both tables
- Clean downgrade that drops both tables
- Revision chain: 019 -> 020
- Original account_block_index table untouched
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic -> PostgreSQL | Migration runs DDL with database owner privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | account_dictionary.account_id | mitigate | UNIQUE constraint prevents duplicate entries; only Rust indexer and migration script write to this table |
| T-15-02 | Denial of Service | Migration downgrade | accept | Downgrade drops v2 tables; acceptable since old table is retained as fallback |
</threat_model>

<verification>
- Migration file imports correctly and has proper revision chain
- Both tables are defined with correct column types (INTEGER, not BIGINT)
- Primary key and indexes are created
- Downgrade reverses all changes cleanly
- Original account_block_index table is completely untouched
</verification>

<success_criteria>
Migration 020 can be loaded as a Python module with correct revision chain (019 -> 020), creates account_dictionary and account_block_index_v2 tables with INTEGER columns and proper indexes, and has a clean downgrade path.
</success_criteria>

<output>
After completion, create `.planning/phases/15-account-block-index-integer-encoding/15-01-SUMMARY.md`
</output>
